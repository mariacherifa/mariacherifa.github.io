<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://mariacherifa.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://mariacherifa.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-17T15:32:59+00:00</updated><id>https://mariacherifa.github.io/feed.xml</id><title type="html">blank</title><subtitle>Maria Cherifa&apos;s website </subtitle><entry><title type="html">Safety Reasoning</title><link href="https://mariacherifa.github.io/blog/2026/Safety/" rel="alternate" type="text/html" title="Safety Reasoning"/><published>2026-06-15T00:00:00+00:00</published><updated>2026-06-15T00:00:00+00:00</updated><id>https://mariacherifa.github.io/blog/2026/Safety</id><content type="html" xml:base="https://mariacherifa.github.io/blog/2026/Safety/"><![CDATA[<h1 id="introduction">Introduction</h1> <p>Until now, we have viewed post-training as a way to shape the conditional distribution</p> \[p_\theta(y\mid u),\] <p>where $u$ is the user prompt and $y$ is the model response.</p> <p>In SFT, we trained the model to imitate demonstrations. In RLHF and DPO, we used preference data so that preferred responses become more likely than rejected ones.</p> <p>Safety reasoning asks a slightly different question. Instead of only asking</p> \[\text{Which response is better?}\] <p>we also ask</p> \[\text{Which responses are admissible?}\] <p>This distinction is important. A response can be fluent, detailed, and even useful for the user’s stated goal, while still being unsafe. For example, if a prompt asks for harmful instructions, the most directly useful answer may also be the response we do not want the model to produce.</p> <p>So the central object is no longer only helpfulness. It is <strong>helpfulness under constraints</strong>.</p> <h1 id="modeling-safety">Modeling safety</h1> <p>Let</p> \[u\in \mathcal{U}\] <p>be a prompt, and let</p> \[y\in \mathcal{Y}\] <p>be a possible response.</p> <p>For each prompt $u$, we can think of the response space as being divided into two regions:</p> \[\mathcal{Y}_{\mathrm{safe}}(u) \subseteq \mathcal{Y},\] <p>the set of the responses that are admissible for the prompt $u$, and</p> \[\mathcal{Y}_{\mathrm{unsafe}}(u)=\mathcal{Y}\setminus \mathcal{Y}_{\mathrm{safe}}(u).\] <p>The set of the responses that should not be produced.</p> <p>This formulation already captures an important point: safety depends on the prompt. The same response can be safe in one context and unsafe in another.</p> <p>For example, a detailed chemical explanation may be safe in an educational context, but unsafe if the prompt asks for operational instructions for harm. Similarly, medical information may be safe when it is general and non-diagnostic, but unsafe if the model presents itself as replacing a clinician.</p> <p>So safety is not simply a property of the response $y$ alone. It is a property of the pair</p> \[(u,y).\] <p>To make this more quantitative, we can introduce a safety cost</p> \[c(u,y)\geq 0.\] <p>The value $c(u,y)$ measures how much the response $y$ violates the safety requirements for prompt $u$. A small value means that the response is safe, while a large value means that the response is unsafe.</p> <p>For example, one can define the safe set through a threshold:</p> \[\left\{y\in \mathcal{Y}: c(u,y)\leq \varepsilon \right\},\] <p>where $\varepsilon\geq 0$ is a tolerance level.</p> <p>In the strictest case, we may take</p> \[\varepsilon = 0,\] <p>so that safe responses are exactly those with zero safety cost.</p> <h1 id="from-helpfulness-to-constrained-helpfulness">From Helpfulness to Constrained Helpfulness</h1> <p>If we only cared about helpfulness, we could imagine choosing response by solving</p> \[y^*(u) \in \arg\max_{y\in \mathcal{Y}} r_{\mathrm{help}}(u,y),\] <p>where</p> \[r_{\mathrm{help}}(u,y)\] <p>is a score measuring how helpful the response $y$ is for the prompt $u$.</p> <p>But this objective is incomplete. It optimizes over all possible responses, including unsafe ones. If the prompt itself is harmful, the response that maximizes helpfulness may be precisely the response that violates the safety constraint.</p> <p>Safety reasoning changes the feasible set. Instead of maximizing helpfulness over all responses, we maximize helpfulness only over admissible responses:</p> \[y^*(u) \in \arg\max_{y\in \mathcal{Y}_{\mathrm{safe}}(u)} r_{\mathrm{help}}(u,y).\] <p>Equivalently, using the safety cost, we can write</p> \[y^*(u) \in \arg\max_{y\in \mathcal{Y}} r_{\mathrm{help}}(u,y)\] <p>subject to</p> \[c(u,y)\leq \varepsilon.\] <p>This is the basic mathematical idea behind safety reasoning:</p> \[\text{maximize helpfulness, but only among safe responses.}\] <p>So safety does not mean removing helpfulness. It means constraining helpfulness.</p> <h1 id="the-distributional-view">The distributional view</h1> <p>The previous formulation was deterministic: for each prompt, we selected one response</p> \[y^*(u).\] <p>But a language model does not directly output a single response by solving an optimization problem over $\mathcal{Y}$. It defines a probability distribution</p> \[p_\theta(y\mid u).\] <p>So the safety question becomes distributional. Instead of asking whether one response is safe, we ask how much probability mass the model assigns to unsafe responses.</p> <p>For a fixed prompt $u$, the probability of generating an unsafe response is</p> \[\sum_{y\in \mathcal{Y}_{\mathrm{unsafe}}(u)} p_{\theta}(y\mid u).\] <p>Ideally, we want this quantity to be small:</p> \[p_\theta \left( \mathcal{Y}_{\mathrm{unsafe}}(u) \mid u \right) \approx 0.\] <p>Equivalently, we want most of the probability mass to lie on safe responses:</p> \[p_\theta \left( \mathcal{Y}_{\mathrm{safe}}(u) \mid u \right) \approx 1.\] <p>This gives a constrained optimization view of safety fine-tuning.</p> <p>Let</p> \[\mathcal{D}_U\] <p>denote the distribution of user prompts. We want to optimize helpfulness on average over prompts and model responses, while keeping the expected safety cost small.</p> <p>A natural constrained objective is</p> \[\max_\theta \mathbb{E}_{u\sim \mathcal{D}_U} \mathbb{E}_{y\sim p\theta(\cdot\mid u)} \left[ r_{\mathrm{help}}(u,y) \right]\] <p>subject to</p> \[\mathbb{E}_{u\sim \mathcal{D}_U} \mathbb{E}_{y\sim p\theta(\cdot\mid u)} \left[ c(u,y) \right] \leq \varepsilon.\] <p>This says that the model should maximize expected helpfulness, while keeping the expected safety violation below a threshold.</p> <p>Another way to write a safety constraint is to constrain the probability of unsafe responses directly:</p> \[\mathbb{E}_{u\sim \mathcal{D}_U} \left[ p\theta \left( \mathcal{Y}_{\mathrm{unsafe}}(u) \mid u \right) \right] \leq \varepsilon.\] <p>Both formulations express the same idea: unsafe behavior should be rare under the model distribution.</p> <h1 id="safety-as-regularization">Safety as Regularization</h1> <p>As usual in constrained optimization, we can move from a constrained problem to a regularized objective.</p> <p>Starting from</p> \[\max_\theta \mathbb{E}_{u\sim \mathcal{D}_U} \mathbb{E}_{y\sim p\theta(\cdot\mid u)} \left[ r_{\mathrm{help}}(u,y) \right]\] <p>subject to</p> \[\mathbb{E}_{u\sim \mathcal{D}_U} \mathbb{E}_{y\sim p\theta(\cdot\mid u)} \left[ c(u,y) \right] \leq \varepsilon,\] <p>we introduce a Lagrange multiplier</p> \[\lambda&gt;0.\] <p>Then the objective becomes</p> \[\max_\theta \mathbb{E}_{u\sim \mathcal{D}_U} \mathbb{E}_{y\sim p\theta(\cdot\mid u)} \left[ r_{\mathrm{safe}}(u,y) \right].\] <p>This formulation makes the tradeoff explicit. Increasing $\lambda$ puts more weight on safety. Decreasing $\lambda$ puts more weight on helpfulness.</p> <p>The goal is not to make the model refuse everything. The goal is to shift probability mass away from unsafe regions while preserving helpful behavior on safe prompts.</p> <h1 id="how-this-appears-in-data">How this appears in Data</h1> <p>The previous formulation is idealized. In practice, we do not know the true helpfulness reward</p> \[r_{\mathrm{help}}(u,y),\] <p>nor do we know the true safety cost</p> \[c(u,y)\] <p>for every possible prompt-response pair.</p> <p>Instead, we observe data.</p> <p>In supervised safety fine-tuning, the dataset contains examples</p> \[(u_i,y_i),\] <p>where $y_i$ is the desired safe response. For safe prompts, this response may be a direct answer. For unsafe prompts, it may be a refusal, a redirection, or a safe high-level explanation.</p> <p>The loss is still the usual negative log-likelihood:</p> \[-\log p_\theta(y_i\mid u_i).\] <p>But the target $y_i$ has changed. It is no longer only a helpful answer. It is a response that encodes the desired safety behavior.</p> <p>In preference-based safety fine-tuning, the dataset contains triples</p> \[(u_i,y_i^+,y_i^-),\] <p>where $y_i^+$ is safer or more appropriate than $y_i^-$. Ideally,</p> \[y_i^+ \in \mathcal{Y}_{\mathrm{safe}}(u_i),\] <p>while</p> \[y_i^- \in \mathcal{Y}_{\mathrm{unsafe}}(u_i)\] <p>or is at least less safe.</p> <p>The preference label says</p> \[y_i^+ \succ y_i^-.\] <p>This fits naturally with DPO. The DPO objective increases the policy-to-reference ratio of the safer response and decreases the policy-to-reference ratio of the unsafe or less appropriate response.</p> <p>So safety can enter post-training through demonstrations, through preferences, or through explicit penalties.</p> <h1 id="what-safety-reasoning-means">What Safety Reasoning means</h1> <p>From this perspective, safety reasoning is not just refusal. It is the model’s ability to reason about the constraint set</p> \[\mathcal{Y}_{\mathrm{safe}}(u).\] <p>Given a prompt $u$, the model should implicitly answer questions such as:</p> \[\text{What is the user asking for?}\] \[\text{Is the request safe?}\] \[\text{If not, what kind of response remains admissible?}\] \[\text{Can I provide a safe alternative?}\] <p>For a safe prompt, safety reasoning should not block helpfulness. The model should answer directly.</p> <p>For an unsafe prompt, safety reasoning should restrict the response space. The model should avoid responses in</p> \[\mathcal{Y}_{\mathrm{unsafe}}(u)\] <p>and instead produce a response in</p> \[\mathcal{Y}_{\mathrm{safe}}(u).\] <p>So the central mathematical idea is:</p> <p>\text{conditional generation under constraints}. $$</p> <p>The model is still generating from</p> \[p_\theta(y\mid u),\] <p>but post-training tries to shape this distribution so that, for safety-sensitive prompts, probability mass is moved away from unsafe responses and toward safe alternatives.</p> <h1 id="conclusion">Conclusion</h1> <p>SFT, RLHF and DPO all modify the conditional distribution</p> \[p_\theta(y\mid u).\] <p>Safety fine-tuning adds a new perspective: some regions of the response space should receive very small probability.</p> <p>Mathematically, we can express this by introducing a safety cost</p> \[c(u,y),\] <p>or equivalently a safe set</p> \[\mathcal{Y}_{\mathrm{safe}}(u).\] <p>The goal is then to optimize helpfulness under safety constraints:</p> \[\text{maximize helpfulness} \quad \text{subject to} \quad \text{safety}.\] <p>Thus, safety reasoning can be understood as constrained conditional generation: the model should remain helpful, but only within the set of admissible responses.</p>]]></content><author><name></name></author><category term="machine-learning"/><category term="transformers"/><category term="attention"/><category term="deep-learning"/><summary type="html"><![CDATA[Introduction Until now, we have viewed post-training as a way to shape the conditional distribution]]></summary></entry><entry><title type="html">DPO and Safety Fine Tuning</title><link href="https://mariacherifa.github.io/blog/2026/DPO-safety-FT/" rel="alternate" type="text/html" title="DPO and Safety Fine Tuning"/><published>2026-05-29T00:00:00+00:00</published><updated>2026-05-29T00:00:00+00:00</updated><id>https://mariacherifa.github.io/blog/2026/DPO-safety-FT</id><content type="html" xml:base="https://mariacherifa.github.io/blog/2026/DPO-safety-FT/"><![CDATA[<h1 id="introduction">Introduction</h1> <p>As seen in the previous post, RLHF has two main components.</p> <p>First, we train a reward model</p> \[r_\phi(u,y)\] <p>from human preference data. Then we optimize the language model policy using this learned reward. This improves over SFT because the model no longer learns only from demonstrations. It also learns from comparisons between answers. However, RLHF also introduces new difficulties. The reward model is trained on a finite preference dataset. Therefore, it may generalize poorly outside the region covered by the data. If the policy is optimized too aggressively against this imperfect reward model, it may find responses that receive high reward but are not actually preferred by humans. This is the problem of <strong>reward hacking</strong>.</p> <p>Moreover, the policy optimization step is technically complex. It usually relies on reinforcement learning algorithms such as PPO. These methods require sampling from the current policy, estimating values or advantages, controlling the KL divergence to a reference model, and carefully tuning the optimization procedure.</p> <p>This motivates a natural question:</p> \[\text{Can we use preference data without explicitly training a reward model and running RL?}\] <p>This is the idea behind Direct Preference Optimization, or DPO. At a high level, DPO replaces the RLHF pipeline</p> \[\text{train a reward model} \quad + \quad \text{run RL policy optimization}\] <p>with a direct objective:</p> \[\text{train the policy directly on preference pairs}.\] <p>So DPO keeps the preference data, but removes the explicit reward-modeling and reinforcement-learning steps.</p> <h1 id="the-bridge-between-rlhf-and-dpo">The Bridge Between RLHF and DPO</h1> <p>The starting point of DPO is the KL-regularized RLHF objective. For a fixed prompt $u$, recall the optimization problem</p> \[\max_{p\in \Delta(\mathcal{Y})} \left\{ \mathbb{E}_{y\sim p(\cdot\mid u)} \left[ r(u,y) \right] - \beta D_{\mathrm{KL}} \left( p(\cdot\mid u) \| p_{\mathrm{ref}}(\cdot\mid u) \right) \right\}.\] <p>Here $p_{\mathrm{ref}}$ is the reference policy, usually the SFT model, and $\beta&gt;0$ controls the strength of the KL penalty.</p> <p>As we saw in the previous post, the solution of this problem is</p> \[p^*(y\mid u) = \frac{ p_{\mathrm{ref}}(y\mid u) \exp\left(r(u,y)/\beta\right) }{ \sum_{y'} p_{\mathrm{ref}}(y'\mid u) \exp\left(r(u,y')/\beta\right) }.\] <p>This equation says that the optimal policy is obtained by tilting the reference policy toward high-reward responses. The key idea of DPO starts from this equation. Instead of learning a reward model separately, DPO asks:</p> \[\text{Can we express the reward directly in terms of the optimal policy?}\] <p>The answer is yes.</p> <p>Starting from the previous formula, we can rearrange terms. We get</p> \[r(u,y) = \beta \log \frac{ p^*(y\mid u) }{ p_{\mathrm{ref}}(y\mid u) } + c(u),\] <p>where</p> \[c(u) = \beta \log \sum_{y'} p_{\mathrm{ref}}(y'\mid u) \exp\left(r(u,y')/\beta\right).\] <p>Therefore, for a fixed prompt, the reward is equivalent up to an additive constant to the log-ratio</p> \[\log \frac{ p^*(y\mid u) }{ p_{\mathrm{ref}}(y\mid u) }.\] <p>This is the bridge between RLHF and DPO.</p> <p>RLHF learns a reward model and then optimizes a policy against it. DPO instead uses the fact that, under the KL-regularized objective, the reward can be represented through the optimal policy itself. So rather than learning</p> \[r_\phi(u,y)\] <p>explicitly, DPO directly trains a policy</p> \[p_\theta(y\mid u)\] <p>whose log-ratio against the reference policy behaves like a reward.</p> <h1 id="the-bradleyterry-preference-model-revisited">The Bradley–Terry Preference Model Revisited</h1> <p>In RLHF, preferences are usually modeled through the Bradley–Terry model:</p> \[\mathbb{P} \left( y^+ \succ y^- \mid u \right) = \sigma \left( r(u,y^+)-r(u,y^-) \right),\] <p>where $y^+$ is the preferred response and $y^-$ is the rejected response. In RLHF $r$ is learned separatly using preference data, but DPO instead replaces $r$ by,</p> \[r(u,y)= \beta \log\frac{p_{\theta}(y\mid u)}{p_{\text{ref}}(y\mid u)} +c(u)\] <p>Substituting this expression into the Bradley–Terry model gives</p> \[\mathbb{P}_{\theta}(y^{+}\succ y^{-} \mid u) = \sigma \left(\beta \left[ \log \frac{p_{\theta}(y^{+}\mid u)}{p_{\text{ref}}(y^{+}\mid u)}-\log \frac{p_{\theta}(y^{-}\mid u)}{p_{\text{ref}}(y^{-}\mid u)} \right] \right)\] <p>So DPO trains the policy so that the preferred response has a larger policy-to-reference log-ratio than the rejected response:</p> \[\log \frac{ p_\theta(y^+\mid u) }{ p_{\mathrm{ref}}(y^+\mid u) } &gt; \log \frac{ p_\theta(y^-\mid u) }{ p_{\mathrm{ref}}(y^-\mid u) }.\] <p>This is the central idea of DPO. The model does not need an explicit reward model. The quantity</p> \[\beta \log \frac{ p_\theta(y\mid u) }{ p_{\mathrm{ref}}(y\mid u) }\] <p>plays the role of an implicit reward.</p> <h1 id="dpo-as-maximum-likelihood-estimation">DPO as Maximum Likelihood Estimation</h1> <p>Let</p> \[\mathcal{D}_{\mathrm{pref}}\] <p>denote the distribution of preference data. A sample from this distribution is a triple</p> \[(u,y^+,y^-)\sim \mathcal{D}_{\mathrm{pref}},\] <p>where (y^+) is preferred to (y^-).</p> <p>Under the DPO preference model, the probability of observing this preference is</p> \[\mathbb{P}_{\theta} \left( y^+ \succ y^- \mid u \right) = \sigma \left( \beta \left[ \log \frac{ p_\theta(y^+\mid u) }{ p_{\mathrm{ref}}(y^+\mid u) } - \log \frac{ p_\theta(y^-\mid u) }{ p_{\mathrm{ref}}(y^-\mid u) } \right] \right).\] <p>Therefore, DPO can be trained by maximum likelihood on preference pairs. Equivalently, we minimize the negative log-likelihood of the observed preferences:</p> \[\mathcal{L}_{\mathrm{DPO}}(\theta) = - \mathbb{E}_{(u,y^+,y^-)\sim \mathcal{D}_{\mathrm{pref}}} \left[ \log \sigma \left( \beta \left[ \log \frac{ p_\theta(y^+\mid u) }{ p_{\mathrm{ref}}(y^+\mid u) } - \log \frac{ p_\theta(y^-\mid u) }{ p_{\mathrm{ref}}(y^-\mid u) } \right] \right) \right].\] <p>This is the DPO objective.</p> <p>The key point is that DPO compares the preferred and rejected responses through their policy-to-reference ratios, not only through their raw probabilities under the policy. More precisely, it compares</p> \[\frac{ p_\theta(y^+\mid u) }{ p_{\mathrm{ref}}(y^+\mid u) } \qquad \text{and} \qquad \frac{ p_\theta(y^-\mid u) }{ p_{\mathrm{ref}}(y^-\mid u) }.\] <p>Thus, DPO encourages the preferred response to become more likely relative to the reference model, and the rejected response to become less likely relative to the reference model. The reference policy acts as an anchor: the model is not only learning which response is better, but also how to move away from the supervised model in a controlled way.</p> <h1 id="toward-safety-fine-tuning">Toward Safety Fine-Tuning</h1> <p>DPO gives a direct way to train a language model from preference pairs. Until now, we have mostly interpreted these preferences as helpfulness preferences: one answer may be clearer, more relevant, or more useful than another.</p> <p>However, preference data can also encode safety. For a given prompt $u$, we may compare a safer response $y^+$ with an unsafe or less appropriate response $y^-$. The preference label</p> \[y^+ \succ y^-\] <p>then tells the model that $y^+$ should be preferred to $y^-$.</p> <p>In the DPO objective, this means increasing the policy-to-reference ratio of $y^+$, while decreasing the policy-to-reference ratio of $y^-$. Therefore, the model is not only learning which response is more helpful. It can also learn which response is more appropriate under safety constraints. This gives a natural bridge from DPO to safety fine-tuning.</p> <p>In safety fine-tuning, post-training is no longer only about making the model more helpful. It is about making the model helpful within a constrained response space.</p> <p>In the next post, we make this idea more explicit by introducing admissible response sets, safety costs, and constrained conditional generation.</p>]]></content><author><name></name></author><category term="machine-learning"/><category term="transformers"/><category term="attention"/><category term="deep-learning"/><summary type="html"><![CDATA[Introduction]]></summary></entry><entry><title type="html">Reinforcement Learning from Human Feedback (RLHF)</title><link href="https://mariacherifa.github.io/blog/2026/RLHF/" rel="alternate" type="text/html" title="Reinforcement Learning from Human Feedback (RLHF)"/><published>2026-05-26T00:00:00+00:00</published><updated>2026-05-26T00:00:00+00:00</updated><id>https://mariacherifa.github.io/blog/2026/RLHF</id><content type="html" xml:base="https://mariacherifa.github.io/blog/2026/RLHF/"><![CDATA[<h1 id="introduction">Introduction</h1> <p>In the previous post, we saw that supervised fine-tuning treats language model post-training as a supervised learning problem. We start from a pretrained model, collect prompt-response pairs, and train the model to assign high probability to the desired responses. This already changes the behavior of the model. Instead of simply continuing text, the model learns to answer prompts in a more assistant-like way.</p> <p>However, SFT has an important limitation: it does not directly model <strong>preferences</strong> between different possible answers.</p> <p>Suppose the prompt is</p> \[u= \text{Define the entropy}\] <p>Now consider two possible responses:</p> \[y_a = \text{Entropy is disorder}\] <p>And,</p> \[y_b= \text{Entropy measures the number of microscopic configurations compatible with a macroscopic state.}\] <p>Both responses are related to the prompt. But many humans would prefer $y_b$, because it is more precise and more informative. The SFT objective does not naturally express this comparison. It can increase the probability of a demonstrated answer, but it does not directly say:</p> \[y_b \succ y_a.\] <p>This is the motivation for reinforcement learning from human feedback, usually abbreviated as RLHF. The goal is no longer only to imitate demonstrations. The goal is to use human preferences to improve the model policy. In this post, we focus on the classical RLHF pipeline: preference data, reward modeling, and policy optimization with a KL penalty.</p> <div class="rlhf-roadmap"> <div class="roadmap-step"> <div class="step-number">1</div> <div class="step-text"> Preference data </div> </div> <div class="roadmap-arrow">↓</div> <div class="roadmap-step"> <div class="step-number">2</div> <div class="step-text"> The reward model </div> </div> <div class="roadmap-arrow">↓</div> <div class="roadmap-step"> <div class="step-number">3</div> <div class="step-text"> The KL-regularized RLHF objective </div> </div> <div class="roadmap-arrow">↓</div> <div class="roadmap-step"> <div class="step-number">4</div> <div class="step-text"> RLHF as constrained optimization </div> </div> <div class="roadmap-arrow">↓</div> <div class="roadmap-step"> <div class="step-number">5</div> <div class="step-text"> Policy-gradient methods and PPO </div> </div> </div> <style>.rlhf-roadmap{max-width:650px;margin:2rem auto;padding:1.5rem;border:1px solid #e5e7eb;border-radius:16px;background:#fafafa;font-family:inherit}.roadmap-step{display:flex;align-items:center;gap:1rem;padding:.9rem 1rem;border-radius:12px;background:white;border:1px solid #e5e7eb}.step-number{width:2rem;height:2rem;border-radius:999px;background:#111827;color:white;display:flex;align-items:center;justify-content:center;font-weight:600;flex-shrink:0}.step-text{font-size:1rem;line-height:1.5}.roadmap-arrow{text-align:center;font-size:1.6rem;line-height:1.6;color:#6b7280}</style> <h1 id="preference-data">Preference data</h1> <p>The first difference between SFT and RLHF is the <strong>type of data</strong>.</p> <p>In SFT, the dataset contains examples of the form</p> \[(u_i,y_i),\] <p>where $u_i$ is a prompt and $y_i$ is a desired answer.</p> <p>In RLHF, we instead use preference data. A typical preference dataset has the form</p> \[(u_i,y_i^+,y_i^-).\] <p>Here $u_i$ is a prompt, $y_i^+$ is the preferred response, and $y_i^-$ is the rejected response.</p> <p>So the preference data says:</p> \[y_i^+ \succ y_i^- \qquad \text{given the prompt } u_i.\] <p>This is a weaker form of supervision than giving an exact score to every answer. The human annotator does not need to say how good a response is in absolute terms. They only need to compare two responses.</p> <p>This is useful because preferences are often easier to collect than absolute scores. It is difficult to say that a response deserves score $7.3$, but it is much easier to say that one answer is better than another.</p> <h1 id="the-reward-model">The reward model</h1> <p>To use preference data, we introduce a reward model. It is a parametric function</p> \[r_{\phi}(u,y) \in \mathbb{R}\] <p>It takes a prompt $u$ and a response $y$, and outputs a scalar reward. The goal is that preferred responses receive larger rewards than rejected responses.</p> <p>So for a preference pair</p> \[(u_i,y_i^+,y_i^-),\] <p>we want</p> \[r_\phi(u_i,y_i^+) &gt; r_\phi(u_i,y_i^-).\] <p>The reward model is not the language model itself. It is a separate model trained to predict human preferences. Usually, the reward model is initialized from a language model or shares a similar transformer architecture, but its output is a single scalar instead of a distribution over next tokens.</p> <h1 id="bradley-terry-model">Bradley-Terry model</h1> <p>Human preferences are noisy. Different annotators may disagree, and even the same annotator may not always make perfectly consistent choices. Therefore, we do not model the preference as deterministic. A common choice is the Bradley–Terry model:</p> \[\mathbb{P}_\phi \left( y_i^+ \succ y_i^- \mid u_i \right) = \sigma \left( r_\phi(u_i,y_i^+) - r_\phi(u_i,y_i^-) \right),\] <p>where</p> \[\sigma(z) = \frac{1}{1+\exp(-z)}\] <p>is the sigmoid function. This model has a simple interpretation. If</p> \[r_\phi(u_i,y_i^+) \gg r_\phi(u_i,y_i^-),\] <p>then</p> \[\mathbb{P}_\phi \left( y_i^+ \succ y_i^- \mid u_i \right) \approx 1.\] <p>If the two rewards are close, then the model is uncertain:</p> \[\mathbb{P}_\phi \left( y_i^+ \succ y_i^- \mid u_i \right) \approx \frac{1}{2}.\] <p>So the reward difference</p> \[r_\phi(u_i,y_i^+) - r_\phi(u_i,y_i^-)\] <p>controls how confident the model is that one response is better than the other.</p> <h1 id="training-the-reward-model">Training the reward model</h1> <p>We now train the reward model from the finite preference dataset</p> \[\{(u_i,y_i^+,y_i^-)\}_{i=1}^{m}.\] <p>Under the Bradley–Terry model, the likelihood of the observed preferences is</p> \[\prod_{i=1}^{m} \sigma \left( r_\phi(u_i,y_i^+) - r_\phi(u_i,y_i^-) \right).\] <p>Equivalently, we minimize the negative log-likelihood:</p> \[\mathcal{L}_{\mathrm{RM}}(\phi) = - \frac{1}{m} \sum_{i=1}^{m} \log \sigma \left( r_\phi(u_i,y_i^+) - r_\phi(u_i,y_i^-) \right).\] <p>This objective encourages the reward model to assign larger rewards to preferred responses than to rejected responses. After minimizing this loss, we obtain a trained reward function</p> \[r_\phi(u,y)\in \mathbb{R}.\] <p>This reward model will then be used to improve the language model.</p> <h1 id="from-reward-modeling-to-policy-optimization">From reward modeling to policy optimization</h1> <p>At this point, we have two objects. First, we have a language model policy</p> \[p_\theta(y\mid u),\] <p>which defines a distribution over responses $y$ given a prompt $u$.</p> <p>Second, we have a reward model</p> \[r_\phi(u,y),\] <p>which assigns a scalar reward to a prompt-response pair.</p> <p>A natural idea is to optimize the policy so that it generates high-reward responses:</p> \[\max_\theta \mathbb{E}_{u\sim \mathcal{D}_{U}} \left[ \mathbb{E}_{y\sim p_\theta(\cdot\mid u)} \left[ r_\phi(u,y) \right] \right].\] <p>Here $\mathcal{D}_{U}$ denotes the distribution of prompts. However, optimizing this objective directly is dangerous. The reward model is trained on a finite preference dataset. It is only an approximation of human preferences. There may be regions where the reward model generalizes poorly. If the policy is optimized too aggressively, it may find responses that exploit mistakes in the reward model instead of genuinely improving according to human preferences. This is known as <strong>reward hacking or overoptimization</strong>.</p> <p>To reduce this problem, RLHF usually regularizes the new policy so that it does not move too far from a reference policy. The reference policy is usually the SFT model:</p> \[p_{\mathrm{ref}}(y\mid u) = p_{\theta_{\mathrm{SFT}}}(y\mid u).\] <p>This makes sense because the SFT model already follows instructions, produces fluent text, and behaves like an assistant. RLHF should improve this model using preferences, not destroy its useful behavior.</p> <h1 id="the-kl-regularized-rlhf-objective">The KL-regularized RLHF objective</h1> <p>The standard RLHF objective is therefore a KL-regularized reward maximization problem:</p> \[J(\theta) = \mathbb{E}_{u\sim \mathcal{D}_{U}} \left[ \mathbb{E}_{y\sim p_\theta(\cdot\mid u)} \left[ r_\phi(u,y) \right] - \beta D_{\mathrm{KL}} \left( p_\theta(\cdot\mid u) \| p_{\mathrm{ref}}(\cdot\mid u) \right) \right].\] <p>The parameter</p> \[\beta&gt;0\] <p>controls the strength of the KL penalty. The KL term is</p> \[D_{\mathrm{KL}} \left( p_\theta(\cdot\mid u) \| p_{\mathrm{ref}}(\cdot\mid u) \right) = \mathbb{E}_{y\sim p_\theta(\cdot\mid u)} \left[ \log \frac{ p_\theta(y\mid u) }{ p_{\mathrm{ref}}(y\mid u) } \right].\] <p>This term penalizes the policy when it puts too much probability on responses that the reference model considered unlikely. So the objective has two competing goals:</p> \[\text{increase reward}\] <p>and</p> \[\text{stay close to the SFT policy}.\] <p>The KL term prevents the model from moving too far into regions where the reward model may be unreliable. So it acts like a trust region around the SFT model.</p> <h1 id="rlhf-as-constrained-optimization">RLHF as constrained optimization</h1> <p>The KL-regularized objective can also be understood as the Lagrangian form of a constrained optimization problem.</p> <p>For a fixed prompt distribution, we can write the constrained problem as</p> \[\max_{\theta} \mathbb{E}_{u\sim \mathcal{D}_{U}} \mathbb{E}_{y\sim p_\theta(\cdot\mid u)} \left[ r_\phi(u,y) \right]\] <p>subject to</p> \[\mathbb{E}_{u\sim \mathcal{D}_{U}} \left[ D_{\mathrm{KL}} \left( p_\theta(\cdot\mid u) \| p_{\mathrm{ref}}(\cdot\mid u) \right) \right] \leq \varepsilon.\] <p>So RLHF can be viewed as reward maximization inside a KL ball around the reference policy.</p> <p>The parameter $\beta$ plays the role of a Lagrange multiplier. A large $\beta$ strongly penalizes movement away from the reference policy. A small $\beta$ allows more aggressive reward optimization.</p> <h1 id="solving-the-kl-regularized-problem-for-a-fixed-prompt">Solving the KL-Regularized Problem for a Fixed Prompt</h1> <p>To understand the role of the KL term more clearly, consider a fixed prompt $u$. We ignore the parametrization of the policy and solve directly over all distributions on responses.</p> <p>Let</p> \[p_0(y) = p_{\mathrm{ref}}(y\mid u),\] <p>and</p> \[r(y) = r_\phi(u,y).\] <p>We want to solve</p> \[\max_{p\in \Delta(\mathcal{Y})} \left\{ \sum_y p(y)r(y) - \beta \sum_y p(y) \log \frac{p(y)}{p_0(y)} \right\},\] <p>subject to</p> \[\sum_y p(y)=1.\] <p>The Lagrangian is</p> \[\mathcal{L}(p,\lambda) = \sum_y p(y)r(y) - \beta \sum_y p(y) \log \frac{p(y)}{p_0(y)} + \lambda \left( \sum_y p(y)-1 \right).\] <p>Differentiating with respect to $p(y)$, we get</p> \[\frac{\partial \mathcal{L}}{\partial p(y)} = r(y) - \beta \left( \log \frac{p(y)}{p_0(y)} + 1 \right) + \lambda.\] <p>At the optimum,</p> \[r(y) - \beta \left( \log \frac{p(y)}{p_0(y)} + 1 \right) + \lambda = 0.\] <p>Therefore,</p> \[p(y) = p_0(y) \exp \left( \frac{r(y)}{\beta} \right) \exp \left( \frac{\lambda}{\beta}-1 \right).\] <p>The last factor is a normalization constant. Using</p> \[\sum_y p(y)=1,\] <p>we obtain</p> \[p^*(y) = \frac{ p_0(y) \exp \left( \frac{r(y)}{\beta} \right) }{ Z },\] <p>where</p> \[Z = \sum_{y'} p_0(y') \exp \left( \frac{r(y')}{\beta} \right).\] <p>So the optimal policy is a tilted version of the reference policy:</p> \[p^*(y) \propto p_{\mathrm{ref}}(y\mid u) \exp \left( \frac{r_\phi(u,y)}{\beta} \right).\] <p>This expression shows that the optimal policy is obtained by tilting the reference policy toward high-reward responses. The reference model provides the base distribution, while the exponential reward term increases the probability of responses with larger reward.</p> <p>A small technical point is that this derivation assumes that the optimized policy is supported on the support of the reference policy. In other words, if</p> \[p_{\mathrm{ref}}(y\mid u)=0,\] <p>then the KL term prevents the optimized policy from assigning positive probability to $y$. The optimization can only redistribute probability mass among responses that are already possible under the reference policy.</p> <p>This formula also makes the role of $\beta$ clear. If $\beta$ is large, then</p> \[\exp \left( \frac{r(y)}{\beta} \right) \approx 1,\] <p>so</p> \[p^*(y) \approx p_0(y).\] <p>The optimal policy stays close to the reference policy.</p> <p>If $\beta$ is small, then the exponential term becomes sharper, and the policy puts much more probability on high-reward responses.</p> <p>Thus, $\beta$ controls the tradeoff:</p> \[\text{large } \beta \quad \Rightarrow \quad \text{conservative update},\] \[\text{small } \beta \quad \Rightarrow \quad \text{aggressive reward optimization}.\] <p><strong>Remark</strong>: during policy optimization, the reward model is frozen. Reward modeling and policy optimization are two separate stages.</p> <h1 id="the-reinforcement-learning-formulation">The Reinforcement Learning Formulation</h1> <p>RLHF is called reinforcement learning from human feedback because, after training the reward model, we can view the language model as a policy in a reinforcement learning problem.</p> <p>In a standard RL problem, an agent interacts with an environment. At each time step, it observes a state, chooses an action, receives a reward, and moves to a new state.</p> <p>For language modeling, the analogy is as follows.</p> <p>The prompt is the initial state:</p> \[s_0 = u.\] <p>At time $t$, the state is the prompt together with the tokens generated so far:</p> \[s_t = (u,y_{&lt;t}).\] <p>The action is the next token:</p> \[a_t = y_t.\] <p>The policy is the language model:</p> \[\pi_\theta(a_t\mid s_t) = p_\theta(y_t\mid u,y_{&lt;t}).\] <p>A full response is a trajectory:</p> \[\tau = (y_1,\dots,y_T).\] <p>The probability of this trajectory under the policy is</p> \[\pi_\theta(\tau\mid u) = \prod_{t=1}^{T} \pi_\theta(y_t\mid u,y_{&lt;t}).\] <p>The reward model assigns a scalar score to the completed response:</p> \[R(u,\tau) = r_\phi(u,y).\] <p>This is usually a terminal reward: the model first generates the full answer, and then the reward model evaluates the prompt-response pair.</p> <p>If we only optimized this reward, the RL objective would be</p> \[\max_\theta \mathbb{E}_{u\sim \mathcal{D}_{U}} \mathbb{E}_{\tau\sim \pi_\theta(\cdot\mid u)} \left[ r_\phi(u,\tau) \right].\] <p>However, in RLHF we do not want the policy to move too far from the reference model. Therefore, we add a KL penalty and use the regularized reward</p> \[R_{\theta}(u,y) = r_\phi(u,y) - \beta \log \frac{ p_\theta(y\mid u) }{ p_{\mathrm{ref}}(y\mid u) }.\] <p>The corresponding RL objective is</p> \[\max_\theta \mathbb{E}_{u\sim \mathcal{D}_{U}} \mathbb{E}_{\tau\sim \pi_\theta(\cdot\mid u)} \left[ R_{\theta}(u,\tau) \right].\] <p>Equivalently, this objective rewards responses that receive a high score from the reward model, while penalizing responses that become too unlikely under the reference policy.</p> <p>Since the language model is autoregressive,</p> \[p_\theta(y\mid u) = \prod_{t=1}^{T} p_\theta(y_t\mid u,y_{&lt;t}),\] <p>and similarly,</p> \[p_{\mathrm{ref}}(y\mid u) = \prod_{t=1}^{T} p_{\mathrm{ref}}(y_t\mid u,y_{&lt;t}).\] <p>Therefore, the KL log-ratio decomposes as</p> \[\log \frac{ p_\theta(y\mid u) }{ p_{\mathrm{ref}}(y\mid u) } = \sum_{t=1}^{T} \log \frac{ p_\theta(y_t\mid u,y_{&lt;t}) }{ p_{\mathrm{ref}}(y_t\mid u,y_{&lt;t}) }.\] <p>This decomposition is useful because the reward model evaluates the completed answer, while the KL penalty can be computed token by token along the generated sequence.</p> <h1 id="policy-optimization-algorithms">Policy Optimization Algorithms</h1> <p>Once RLHF is written as a reinforcement learning problem, we still need an algorithm to optimize the policy. For a fixed prompt $u$, the KL-regularized objective can be written as</p> \[J(\theta) = \mathbb{E}_{y\sim p_\theta(\cdot\mid u)} \left[ r_\phi(u,y) - \beta \log \frac{ p_\theta(y\mid u) }{ p_{\mathrm{ref}}(y\mid u) } \right].\] <p>Equivalently,</p> \[J(\theta) = \sum_{y\in \mathcal{Y}} p_\theta(y\mid u) \left[ r_\phi(u,y) - \beta \log \frac{ p_\theta(y\mid u) }{ p_{\mathrm{ref}}(y\mid u) } \right].\] <p>Here $\mathcal{Y}$ is the set of all possible responses. If the vocabulary has size $V$ and the response length is $T$, then $\mathcal{Y}$ is of order $V^T$. Therefore, computing this sum exactly is impossible. A natural idea is to use sampling. We sample responses from the current policy:</p> \[y^{(1)},\dots,y^{(N)} \sim p_\theta(\cdot\mid u).\] <p>Then we approximate the objective by Monte Carlo:</p> \[\widehat J(\theta) = \frac{1}{N} \sum_{i=1}^{N} \left[ r_\phi(u,y^{(i)}) - \beta \log \frac{ p_\theta(y^{(i)}\mid u) }{ p_{\mathrm{ref}}(y^{(i)}\mid u) } \right].\] <p>However, optimizing the policy requires an estimate of</p> \[\nabla_\theta J(\theta).\] <p>We cannot simply differentiate through the sampled responses $y^{(i)}$, because the responses are discrete sequences of tokens. The sampling operation is not differentiable in the usual sense. The standard solution is to use the log-derivative trick:</p> \[\nabla_\theta p_\theta(y\mid u) = p_\theta(y\mid u) \nabla_\theta \log p_\theta(y\mid u).\] <p>Let</p> \[R_\theta(u,y) = r_\phi(u,y) - \beta \log \frac{ p_\theta(y\mid u) }{ p_{\mathrm{ref}}(y\mid u) }.\] <p>Then the objective is</p> \[J(\theta) = \sum_{y\in\mathcal{Y}} p_\theta(y\mid u) R_\theta(u,y).\] <p>Differentiating gives</p> \[\nabla_\theta J(\theta) = \sum_{y\in\mathcal{Y}} p_\theta(y\mid u) \left[ R_\theta(u,y)-\beta \right] \nabla_\theta \log p_\theta(y\mid u).\] <p>The extra term $-\beta$ appears because $R_\theta(u,y)$ itself depends on $\theta$ through the KL penalty.</p> <p>Thus,</p> \[\nabla_\theta J(\theta) = \mathbb{E}_{y\sim p_\theta(\cdot\mid u)} \left[ \left( r_\phi(u,y) - \beta \log \frac{ p_\theta(y\mid u) }{ p_{\mathrm{ref}}(y\mid u) } - \beta \right) \nabla_\theta \log p_\theta(y\mid u) \right].\] <p>This gives the Monte Carlo policy-gradient estimator</p> \[\nabla_\theta \widehat J(\theta) = \frac{1}{N} \sum_{i=1}^{N} \left[ r_\phi(u,y^{(i)}) - \beta \log \frac{ p_\theta(y^{(i)}\mid u) }{ p_{\mathrm{ref}}(y^{(i)}\mid u) } - \beta \right] \nabla_\theta \log p_\theta(y^{(i)}\mid u).\] <p>This is the basic policy-gradient estimator. It gives a way to update the model even though the sampled responses are discrete. The important point is that we do not differentiate through the text itself. Once a response $y^{(i)}$ has been sampled, it is treated as fixed. What we differentiate is the log-probability that the model assigned to that response:</p> \[\log p_\theta(y^{(i)}\mid u).\] <p>So the update has a simple interpretation. If a sampled response receives a large regularized reward, we increase the probability of generating similar responses. If it receives a small reward, or if it moves too far from the reference policy, we decrease its probability.</p> <p>This gives the core idea behind RLHF policy optimization:</p> \[\text{sample responses} \quad \longrightarrow \quad \text{score them with the reward model} \quad \longrightarrow \quad \text{update the policy probabilities}.\] <p>In practice, this basic estimator can be noisy and unstable. Algorithms such as REINFORCE, actor-critic methods, TRPO, and PPO are different ways of making this update more stable. In RLHF, PPO has historically been used because it limits how much the policy can change at each optimization step.</p> <h1 id="ppo-in-rlhf">PPO in RLHF</h1> <p>The basic policy-gradient estimator gives a direction in which to update the language model policy. However, in practice, this update can be unstable. A single gradient step may change the policy too much, pushing the model into regions where the reward model is unreliable.</p> <p>PPO, which stands for Proximal Policy Optimization, is designed to make these updates more conservative.</p> <p>The main idea is:</p> \[\text{improve the policy, but keep the new policy close to the old one.}\] <p>Suppose responses are generated using an old policy</p> \[\pi_{\theta_{\mathrm{old}}}.\] <p>After collecting these samples, we update the parameters and obtain a new policy</p> \[\pi_\theta.\] <p>For a generated token $a_t$ in state $s_t$, PPO compares the probability of this token under the new policy and under the old policy:</p> \[\rho_t(\theta) = \frac{ \pi_\theta(a_t\mid s_t) }{ \pi_{\theta_{\mathrm{old}}}(a_t\mid s_t) }.\] <p>This ratio measures how much the update changes the probability of the sampled token. If $\rho_t(\theta)&gt;1$, the token has become more likely under the new policy. If $\rho_t(\theta)&lt;1$, it has become less likely.</p> <p>Now introduce the advantage $A_t$. The advantage tells us whether the sampled action was better or worse than expected.</p> <p><strong>Remark.</strong> In practice, the advantage $A_t$ is estimated, not given directly. RLHF implementations usually train a value model $V(s_t)$ to predict how good a state is, and define the advantage as the difference between the observed return and this prediction:</p> \[A_t \approx G_t - V(s_t).\] <p>So $A_t$ measures whether the sampled token was better or worse than expected.</p> \[A_t&gt;0 \quad \Rightarrow \quad \text{increase the probability of } a_t,\] <p>and</p> \[A_t&lt;0 \quad \Rightarrow \quad \text{decrease the probability of } a_t.\] <p>A basic policy-gradient update would therefore use the term</p> \[\rho_t(\theta)A_t.\] <p>The problem is that this term can push the policy too far. PPO avoids this by clipping the ratio. Define</p> \[\bar{\rho}_t(\theta) = \min \left( \max \left( \rho_t(\theta), 1-\epsilon \right), 1+\epsilon \right),\] <p>so that</p> \[\bar{\rho}_t(\theta) \in [1-\epsilon,1+\epsilon].\] <p>The PPO clipped objective is</p> \[\mathcal{L}_{\mathrm{PPO}}(\theta) = \mathbb{E} \left[ \min \left( \rho_t(\theta)A_t, \bar{\rho}_t(\theta)A_t \right) \right].\] <p>Here $\epsilon&gt;0$ is a small hyperparameter. The minimum chooses the more conservative value between the unclipped objective and the clipped objective.</p> <p>If $A_t&gt;0$, PPO allows the model to increase the probability of the token, but not beyond the clipping range. If (A_t&lt;0), PPO allows the model to decrease the probability of the token, but again only within the clipping range.</p> <p>Thus, PPO can be summarized as:</p> \[\text{increase the probability of good tokens, but not too much;}\] \[\text{decrease the probability of bad tokens, but not too much.}\] <p>This is why PPO makes policy optimization more stable.</p> <p>In RLHF, PPO is usually combined with a KL penalty to the reference model. These two mechanisms are related, but they play different roles.</p> <p>PPO clipping controls the size of one local update:</p> \[\pi_{\theta_{\mathrm{old}}} \quad \longrightarrow \quad \pi_\theta.\] <p>It keeps the new policy close to the old policy that generated the current batch of samples.</p> <p>The KL penalty controls the drift from the reference model:</p> \[p_{\mathrm{ref}} \quad \longrightarrow \quad p_\theta.\] <p>It keeps the RLHF policy close to the SFT model.</p> <p>So PPO stabilizes the local optimization step, while the KL penalty keeps the overall policy anchored to the supervised model.</p> <h1 id="summary-of-the-rlhf-pipeline">Summary of the RLHF Pipeline</h1> <p>We can now summarize the RLHF pipeline.</p> <p>The starting point is an SFT model</p> \[p_{\theta_{\mathrm{SFT}}}(y\mid u).\] <p>This model already behaves like an assistant because it was trained on prompt-response demonstrations.</p> <p>Then RLHF adds a preference-learning step.</p> <p>First, for each prompt, we sample or collect several possible responses. Humans compare them and produce preference data of the form</p> \[(u_i,y_i^+,y_i^-).\] <p>Second, we train a reward model</p> \[r_\phi(u,y)\] <p>so that preferred responses receive larger rewards than rejected responses:</p> \[r_\phi(u_i,y_i^+) &gt; r_\phi(u_i,y_i^-).\] <p>This is done using the Bradley–Terry loss</p> \[\mathcal{L}_{\mathrm{RM}}(\phi) = - \frac{1}{m} \sum_{i=1}^{m} \log \sigma \left( r_\phi(u_i,y_i^+) - r_\phi(u_i,y_i^-) \right).\] <p>Third, we optimize the language model policy using the trained reward model. The objective is</p> \[J(\theta) = \mathbb{E}_{u\sim \mathcal{D}_{U}} \left[ \mathbb{E}_{y\sim p_\theta(\cdot\mid u)} \left[ r_\phi(u,y) \right] - \beta D_{\mathrm{KL}} \left( p_\theta(\cdot\mid u) \| p_{\mathrm{ref}}(\cdot\mid u) \right) \right].\] <p>Usually,</p> \[p_{\mathrm{ref}} = p_{\theta_{\mathrm{SFT}}}.\] <p>So the policy is pushed toward high-reward responses, but it is penalized if it moves too far from the SFT model.</p> <p>This gives the central tradeoff of RLHF:</p> \[\text{maximize human preference reward}\] <p>while</p> \[\text{remaining close to the supervised model}.\] <h1 id="limitations-of-rlhf">Limitations of RLHF</h1> <p>RLHF is powerful, but it is not perfect.</p> <p>The first limitation is that the reward model is only an approximation of human preferences. If the reward model makes mistakes, the policy can exploit those mistakes. This is why KL regularization is important.</p> <p>The second limitation is that human preferences can be inconsistent. Different annotators may prefer different styles, levels of detail, or kinds of answers. The reward model learns an average of these preferences.</p> <p>The third limitation is that preference data is not the same as truth. A response can be preferred because it sounds confident or well-written, even if it is not fully correct. So RLHF can improve style and helpfulness, but it does not automatically guarantee factual accuracy.</p> <p>The fourth limitation is that PPO-style optimization is technically complex. It requires sampling, reward modeling, value estimation, KL control, and careful hyperparameter tuning.</p> <p>So RLHF improves the model, but it also introduces new objects:</p> \[\text{a reward model,} \qquad \text{a reference policy,} \qquad \text{a policy optimization algorithm.}\] <h1 id="conclusion">Conclusion</h1> <p>The main idea of RLHF is to move beyond imitation. SFT trains the model to reproduce desired answers. RLHF trains the model to prefer better answers. So the transition from SFT to RLHF can be summarized as:</p> \[\text{maximum likelihood on demonstrations} \quad \longrightarrow \quad \text{reward maximization under preference feedback}.\] <p>This is why RLHF became a central post-training method for assistant-like language models: it gives a way to incorporate human judgments directly into the training process. However, RLHF is technically complex because it requires a reward model and an RL optimization loop. This motivates direct preference optimization methods, such as DPO, which use preference data more directly. This will be the topic of the next post.</p>]]></content><author><name></name></author><category term="machine-learning"/><category term="transformers"/><category term="post-training"/><category term="deep-learning"/><summary type="html"><![CDATA[Introduction In the previous post, we saw that supervised fine-tuning treats language model post-training as a supervised learning problem. We start from a pretrained model, collect prompt-response pairs, and train the model to assign high probability to the desired responses. This already changes the behavior of the model. Instead of simply continuing text, the model learns to answer prompts in a more assistant-like way.]]></summary></entry><entry><title type="html">Supervised Fine Tuning (SFT)</title><link href="https://mariacherifa.github.io/blog/2026/SFT-post-training/" rel="alternate" type="text/html" title="Supervised Fine Tuning (SFT)"/><published>2026-05-22T00:00:00+00:00</published><updated>2026-05-22T00:00:00+00:00</updated><id>https://mariacherifa.github.io/blog/2026/SFT-post-training</id><content type="html" xml:base="https://mariacherifa.github.io/blog/2026/SFT-post-training/"><![CDATA[<h1 id="introduction">Introduction</h1> <p>In the previous post, we described the decoder-only Transformer architecture and the next-token prediction objective. We saw that an autoregressive language model takes a sequence of tokens and, at each position, outputs a probability distribution over the next token. This raises a natural question. If a model is trained only to predict the next token, why does it behave like an assistant?</p> <p>A pretrained language model learns to continue text. But an assistant is expected to do something more specific: follow instructions, answer clearly, adapt to a user, refuse some unsafe requests, and maintain a conversational format. These behaviors are not guaranteed by next-token prediction alone. This is the role of <strong>post-training</strong>.</p> <p>Post-training refers to the set of training procedures applied after pretraining in order to shape the behavior of a language model. In this post, we focus on the first and most basic post-training step: supervised fine-tuning, usually abbreviated as SFT.</p> <h1 id="the-starting-point-a-pretrained-autoregressive-model">The Starting Point: A Pretrained Autoregressive Model</h1> <p>Let</p> \[\mathcal{V}=\lbrace v_1,\dots,v_V\rbrace\] <p>be a vocabulary of size $V$, and let</p> \[x=(x_1,\dots,x_T)\in \lbrace 1,\dots,V\rbrace^T\] <p>be a tokenized sequence. A decoder-only autoregressive Transformer defines, for every position $t$, a conditional probability distribution</p> \[p_\theta(x_{t+1}\mid x_{\leq t}).\] <p>After the Transformer stack has computed the hidden representation $h_t$ at position $t$, a final linear layer maps this representation to a vector of logits</p> \[\ell_t = W_{\text{out}}h_t + b_{\text{out}} \in \mathbb{R}^V.\] <p>The coordinate $\ell_{t,j}$ is the score assigned to token $j$ in the vocabulary. Applying the softmax function gives the next-token distribution:</p> \[p_\theta(x_{t+1}=j \mid x_{\leq t}) = \frac{\exp(\ell_{t,j})} {\sum_{k=1}^{V}\exp(\ell_{t,k})}.\] <p>During pretraining, the parameters $\theta$ are optimized so that the model assigns high probability to the actual next token in large text corpora. The corresponding loss is the negative log-likelihood</p> \[\mathcal{L}_{\mathrm{pretrain}}(\theta) = - \sum_{t=1}^{T} \log p_\theta(x_t \mid x_{&lt;t}).\] <p>Thus, pretraining teaches the model to continue text. The training data may contain books, websites, code, papers, forums, documentation, and many other kinds of text. As a result, the model learns a broad distribution over language: grammar, facts, reasoning patterns, styles, and common ways in which text continues.</p> <p>But <strong>a good text continuation model is not necessarily a good assistant</strong>. To see why, suppose the prompt is</p> \[u = \text{Explain the spectral theorem.}\] <p>A pretrained language model treats this prompt as a prefix and tries to continue it in a statistically plausible way. Many continuations may be reasonable under the pretraining distribution, for example:</p> <ul> <li>This question was asked in an exam …</li> <li>See Chapter 7 for a proof …</li> <li>The spectral theorem is one of the most important results in linear algebra …</li> </ul> <p>All of these may be plausible continuations of text. But if the model is used as an assistant, we want a more specific behavior: we want the model to recognize the prompt as a request and produce a clear, useful answer. This is the central motivation for supervised fine-tuning. Pretraining learns a broad distribution over text. SFT modifies the model so that user prompts are more often followed by assistant-like responses.</p> <h1 id="prompt-response-data">Prompt-Response Data</h1> <p>Supervised fine-tuning uses a dataset of prompt-response pairs. We write this dataset as</p> \[\{(u_i,y_i)\}_{i=1}^n.\] <p>Here,</p> \[u_i= \text{prompt, instruction, or conversation context},\] <p>and</p> \[y_i= \text{desired assistant response.}\] <p>The important point is that the response $y_i$ is not arbitrary text. It is intended to represent the kind of behavior we want from the assistant: clear, useful, correct, and adapted to the prompt. Let</p> \[y_{i}=(y_{i,1},\dots,y_{i,m_{i}})\] <p>be a tokenized response associated with prompt $u_i$. Since the model is still autoregressive, the conditional probability of the full response is factorized token by token:</p> \[p_{\theta}(y_i\mid u_i)=\prod_{t=1}^{m_{i}} p_{\theta}(y_{i,t}\mid u_i,y_{i,&lt;t})\] <p>where</p> \[y_{i,&lt;t}=(y_{i,1},\dots,y_{i,t-1})\] <p>is the previous response tokens to the prompt $u_i$.</p> <p>This is the key point. SFT does not change the autoregressive nature of the model. At every step, the model still predicts the next token. What changes is the training distribution. During pretraining, the model predicts tokens in generic text. During SFT, the model predicts answer tokens conditioned on prompts.</p> <h1 id="the-sft-objective">The SFT objective</h1> <p>For one example $(u_i,y_i)$, the negative log-likelihood loss is:</p> \[\mathcal{L}_{\mathrm{SFT}}^{(i)}(\theta) = - \sum_{t=1}^{m_i} \log p_\theta \left( y_{i,t} \mid u_i, y_{i,&lt;t} \right).\] <p>Averaging over the dataset gives the supervised fine-tuning objective</p> \[\mathcal{L}_{\mathrm{SFT}}(\theta) = - \frac{1}{n} \sum_{i=1}^{n} \sum_{t=1}^{m_i} \log p_\theta \left( y_{i,t} \mid u_i, y_{i,&lt;t} \right).\] <p>Thus, SFT is maximum likelihood training on curated prompt-response examples. The model is encouraged to assign high probability to the target response tokens. If the model assigns low probability to a token appearing in the desired answer, the loss increases. Training then adjusts the parameters so that similar responses become more likely in similar contexts.</p> <p>The parameters $\theta$ are initialized from the pretrained model:</p> \[\theta \leftarrow \theta_0,\] <p>where $\theta_0$ denotes the parameters obtained after pretraining. SFT then continues gradient-based optimization, but now using the supervised dataset $\mathcal D_{\mathrm{SFT}}$.</p> <h1 id="what-changes-during-sft">What changes during SFT?</h1> <p>It is important to notice what does <strong>not</strong> change. The Transformer architecture is the same. The model still uses token embeddings, positional information, causal self-attention, MLP blocks, residual connections, layer normalization, and a final softmax over the vocabulary. The next-token prediction mechanism is also the same. What changes is the data distribution and therefore the behavior encouraged by the loss. During pretraining, the model sees sequences sampled from a broad text distribution, which we can denote informally by</p> \[x \sim \mathcal{D}_{\text{pretrain}}.\] <p>It learns to predict,</p> \[x_t \text{ from } x_{&lt;t}\] <p>During SFT, the model sees formatted prompt-response examples</p> \[z_i=(u_i,y_i)\] <p>where $u_i$ is the prompt and $y_i$ is the target response. For response tokens, the prediction problem becomes</p> \[y_{i,t} \text{ from } (u_i,y_{i,&lt;t})\] <p>Thus, SFT changes the model from a distribution $p_{\theta_{0}}(\cdot\mid \text{generic prefix})$ toward a distribution $p_{\theta_{\text{SFT}}}(\cdot\mid \text{user prompt})$ that assigns more mass to assistant-like continuations.</p> <h1 id="sft-as-behavior-cloning">SFT as behavior cloning</h1> <p>One useful interpretation of SFT is behavior cloning. In reinforcement learning language, a policy maps a context to a distribution over actions. For a language model, the context is a prompt and the action is a sequence of tokens. We can therefore write the language model as a policy</p> \[\pi_\theta(y\mid u) = p_\theta(y\mid u).\] <p>The SFT dataset contains demonstrations of the behavior we want:</p> \[(u_i,y_i) \in \mathcal{D}_{\text{SFT}}\] <p>The model is trained to imitate these demonstrations. Given prompt $u_i$, it should assign high probability to the demonstrated response $y_i$.</p> <p>Since $y_i$ is a sequence, the imitation happens token by token:</p> \[\pi_\theta(y_i\mid u_i) = \prod_{t=1}^{m_i} \pi_\theta(y_{i,t}\mid u_i,y_{i,&lt;t}).\] <p>This is why SFT can be understood as behavior cloning for language models. We provide examples of the desired assistant behavior, and the model learns to reproduce that behavior.</p> <h1 id="sft-as-empirical-risk-minimization">SFT as Empirical Risk Minimization</h1> <p>A second viewpoint is the standard learning theory one. Assume that prompt-response pairs are sampled from some unknown distribution</p> \[(u,y)\sim \mathcal D_{\mathrm{SFT}}.\] <p>This distribution represents the behavior we want the assistant to learn. It may contain helpful answers, polite refusals, mathematical explanations, code solutions, summaries, or multi-turn conversations.</p> <p>Ideally, we would like to minimize the population risk</p> \[\mathcal R(\theta)= \mathbb E_{(u,y)\sim \mathcal D_{\mathrm{SFT}}}[-\log(p_{\theta}(y\mid u))].\] <p>Using the autoregressive factorization, this becomes</p> \[\mathcal{R}(\theta) = \mathbb{E}_{(u,y)\sim\mathcal{D}_{\mathrm{SFT}}} \left[ - \sum_{t=1}^{m} \log p_\theta\left(y_t \mid u, y_{&lt;t}\right) \right].\] <p>The true distribution $\mathcal D_{\mathrm{SFT}}$ is unknown. We only observe a finite dataset ${(u_i,y_i)}_{i=1}^n$. Therefore, we replace the expectation by an empirical average. This gives</p> \[\widehat{\mathcal R}(\theta) = - \frac{1}{n} \sum_{i=1}^{n} \sum_{t=1}^{m_i} \log p_\theta\left(y_{i,t}\mid u_i, y_{i,&lt;t}\right).\] <p>Thus,</p> \[\theta_{\mathrm{SFT}} \in \arg\min_\theta \widehat{\mathcal R}(\theta).\] <p>This shows that SFT is empirical risk minimization with the token-level cross-entropy loss. This viewpoint is useful because it also reveals a limitation: SFT does not directly optimize abstract properties such as truthfulness, harmlessness, or usefulness. It minimizes a supervised loss on a finite set of demonstrations. If the demonstrations are high quality, the model can learn good behavior. If they are narrow, inconsistent, or biased, the model can inherit these limitations.</p> <h1 id="sft-and-kl-divergence">SFT and KL Divergence</h1> <p>There is another useful way to understand the SFT objective: through KL divergence. This point of view is important because KL divergence will appear again later in RLHF. In SFT, it appears naturally from the relationship between maximum likelihood, cross-entropy, and KL divergence.</p> <p>Recall that</p> \[\mathcal{D}_{\mathrm{SFT}}\] <p>denotes the joint distribution over prompt-response pairs, and</p> \[\mathcal{D}_{U}\] <p>denotes its marginal distribution over prompts. Thus, a sample from the SFT distribution is a pair</p> \[(u,y)\sim \mathcal{D}_{\mathrm{SFT}}.\] <p>For a fixed prompt $u$, let</p> \[p(y\mid u) = \mathcal{D}_{\mathrm{SFT}}(y\mid u)\] <p>be the conditional distribution of desired responses in the SFT data, and let</p> \[q_\theta(y\mid u) = p_\theta(y\mid u)\] <p>be the conditional distribution defined by the model.</p> <p>For this fixed prompt, the SFT loss is the expected negative log-likelihood of the desired response:</p> \[\mathbb{E}_{y\sim p(\cdot\mid u)} \left[ -\log q_\theta(y\mid u) \right].\] <p>Writing this expectation explicitly gives</p> \[\mathbb{E}_{y\sim p(\cdot\mid u)} \left[ -\log q_\theta(y\mid u) \right] = - \sum_y p(y\mid u) \log q_\theta(y\mid u).\] <p>This quantity is the cross-entropy between the target distribution $p(\cdot\mid u)$ and the model distribution $q_\theta(\cdot\mid u)$:</p> \[H\left(p(\cdot\mid u),q_\theta(\cdot\mid u)\right) = - \sum_y p(y\mid u) \log q_\theta(y\mid u).\] <p>Therefore, the population SFT risk can be written as</p> \[\mathcal R(\theta) = \mathbb{E}_{u\sim \mathcal{D}_{U}} \left[ H\left( p(\cdot\mid u), q_\theta(\cdot\mid u) \right) \right].\] <p>Now recall the standard cross-entropy identity between two distributions:</p> \[H(p,q) = H(p) + D_{\mathrm{KL}}(p\|q).\] <p>Applying this identity for each prompt $u$, we obtain</p> \[\mathcal R(\theta) = \mathbb{E}_{u\sim \mathcal{D}_{U}} \left[ H\left(p(\cdot\mid u)\right) + D_{\mathrm{KL}} \left( p(\cdot\mid u) \| q_\theta(\cdot\mid u) \right) \right].\] <p>The first term,</p> \[H\left(p(\cdot\mid u)\right),\] <p>is the entropy of the SFT response distribution for the prompt $u$. It depends only on the data distribution $\mathcal{D}_{\mathrm{SFT}}$, not on the model parameters $\theta$.</p> <p>Therefore, minimizing $\mathcal R(\theta)$ with respect to $\theta$ is equivalent to minimizing</p> \[\mathbb{E}_{u\sim \mathcal{D}_{U}} \left[ D_{\mathrm{KL}} \left( p(\cdot\mid u) \| q_\theta(\cdot\mid u) \right) \right].\] <p>In other words, SFT tries to make the model distribution</p> \[q_\theta(\cdot\mid u) = p_\theta(\cdot\mid u)\] <p>close to the demonstration distribution</p> \[p(\cdot\mid u) = \mathcal{D}_{\mathrm{SFT}}(\cdot\mid u).\] <p>The direction of the KL divergence is important. SFT minimizes</p> \[D_{\mathrm{KL}} \left( \mathcal{D}_{\mathrm{SFT}}(\cdot\mid u) \| p_\theta(\cdot\mid u) \right).\] <p>This is the forward KL direction. It penalizes the model when it assigns low probability to responses that appear in the SFT data. So SFT encourages the model to cover the demonstrated behavior.</p> <p>This matches the imitation-learning interpretation. The dataset shows the model examples of desired behavior, and the loss increases the probability of these demonstrated responses.</p> <p>In practice, however, we do not have access to the full distribution $\mathcal{D}_{\mathrm{SFT}}$. We only observe a finite sample</p> \[\{(u_i,y_i)\}_{i=1}^{n}.\] <p>The finite sample induces an empirical distribution over prompt-response pairs:</p> \[\widehat{\mathcal{D}}_{\mathrm{SFT}}(u,y)= \frac{1}{n} \sum_{i=1}^{n} \mathbf{1}\{(u,y)=(u_i,y_i)\}.\] <p>For a fixed prompt $u$, it also defines an empirical conditional distribution over responses $\widehat{\mathcal{D}}_{\mathrm{SFT}}(y\mid u)$. If the same prompt appears several times in the dataset, possibly with different responses, then</p> \[\widehat{\mathcal{D}}_{\mathrm{SFT}}(y\mid u)= \frac{ \sum_{i=1}^{n} \mathbf{1}\{u_i=u,\; y_i=y\} }{ \sum_{i=1}^{n} \mathbf{1}\{u_i=u\} }.\] <p>If each prompt appears only once, then for the training prompt $u_i$, the empirical conditional distribution is concentrated on the demonstrated response $y_i$:</p> \[\widehat{\mathcal{D}}_{\mathrm{SFT}}(y\mid u_i) = \mathbf{1}\{y=y_i\}.\] <p>Now the empirical SFT risk is</p> \[\widehat{\mathcal R}(\theta) = - \frac{1}{n} \sum_{i=1}^{n} \log p_\theta(y_i\mid u_i).\] <p>This can also be written as a cross-entropy under the empirical distribution:</p> \[\widehat{\mathcal R}(\theta) = \mathbb{E}_{u\sim \widehat{\mathcal{D}}_{U}} \left[ H\left( \widehat{\mathcal{D}}_{\mathrm{SFT}}(\cdot\mid u), p_\theta(\cdot\mid u) \right) \right].\] <p>where $\widehat{\mathcal{D}}_{U}$ is the empirical marginal distribution of prompts. Using the cross-entropy identity, we get</p> \[\widehat{\mathcal R}(\theta) = \mathbb{E}_{u\sim \widehat{\mathcal{D}}_{U}} \left[ H\left( \widehat{\mathcal{D}}_{\mathrm{SFT}}(\cdot\mid u) \right) + D_{\mathrm{KL}} \left( \widehat{\mathcal{D}}_{\mathrm{SFT}}(\cdot\mid u) \| p_\theta(\cdot\mid u) \right) \right].\] <p>Therefore, minimizing the empirical SFT risk is equivalent to minimizing</p> \[\mathbb{E}_{u\sim \widehat{\mathcal{D}}_{U}} \left[ D_{\mathrm{KL}} \left( \widehat{\mathcal{D}}_{\mathrm{SFT}}(\cdot\mid u) \| p_\theta(\cdot\mid u) \right) \right].\] <p>So even in the empirical version, SFT can be viewed as KL minimization. The model distribution $p_\theta(\cdot\mid u)$ is trained to approximate the empirical demonstration distribution $\widehat{\mathcal{D}}_{\mathrm{SFT}}(\cdot\mid u)$. In the common case where there is only one demonstrated response $y_i$ for a prompt $u_i$, we have</p> \[\widehat{\mathcal{D}}_{\mathrm{SFT}}(\cdot\mid u_i) = \delta_{y_i}.\] <p>Then the KL divergence becomes</p> \[D_{\mathrm{KL}} \left( \delta_{y_i} \| p_\theta(\cdot\mid u_i) \right) = -\log p_\theta(y_i\mid u_i).\] <p>Thus, the usual negative log-likelihood loss is exactly the KL divergence from the empirical demonstration distribution to the model distribution, up to constants independent of $\theta$.</p> <p>Finally, using the autoregressive factorization,</p> \[p_\theta(y_i\mid u_i) = \prod_{t=1}^{m_i} p_\theta(y_{i,t}\mid u_i,y_{i,&lt;t}),\] <p>we recover the token-level SFT objective:</p> \[\widehat{\mathcal R}(\theta) = - \frac{1}{n} \sum_{i=1}^{n} \sum_{t=1}^{m_i} \log p_\theta(y_{i,t}\mid u_i,y_{i,&lt;t}).\] <p>So the KL view and the next-token view are not two different objectives. They are two ways of looking at the same training criterion.</p> <p>The next-token view says:</p> \[\text{increase the probability of the demonstrated response tokens.}\] <p>The KL view says:</p> \[\text{move the model distribution closer to the SFT demonstration distribution.}\] <p>This perspective will be useful when we discuss RLHF. In RLHF, we will not only imitate demonstrations. We will also optimize the model using human preferences. But while doing so, we usually want to prevent the model from moving too far away from the SFT model. KL divergence will then appear again, not only as an interpretation of the loss, but as an explicit regularization term.</p> <h1 id="masked-loss-on-response-tokens">Masked Loss on response tokens</h1> <p>So far, we have described SFT at the level of prompt-response pairs and empirical risk minimization. However, the Transformer itself does not directly take a pair $(u_i,y_i)$ as two separate mathematical objects. It takes a single sequence of tokens as input. Therefore, in practice, each prompt-response pair is formatted as one sequence:</p> \[z_i = (u_i,y_i).\] <p>The prompt is used as context, but only the response is treated as the target. We encode this with a mask:</p> \[M_{i,t} = \begin{cases} 0, &amp; \text{if token } z_{i,t} \text{ belongs to the prompt},\\ 1, &amp; \text{if token } z_{i,t} \text{ belongs to the response}. \end{cases}\] <p>A minibatch loss can then be written as</p> \[\mathcal L_B(\theta) = - \frac{1}{\sum_{i\in B}\sum_t M_{i,t}} \sum_{i\in B} \sum_t M_{i,t} \log p_\theta(z_{i,t+1}\mid z_{i,\leq t}).\] <p>The prompt tokens are used as context, but the model is not penalized for failing to reproduce the prompt. The model is trained to generate the response conditioned on the prompt. This distinction is important. SFT is not asking the model to copy the user. It is asking the model to produce the assistant side of the conversation.</p> <h1 id="minimal-sft-algorithm">Minimal SFT Algorithm</h1> <p>We can summarize supervised fine-tuning (SFT) as follows.</p> <p>Given:</p> \[\theta_0 = \text{pretrained model parameters},\] <p>and</p> \[\{(u_i,y_i)\}_{i=1}^n.\] <p>Initialize:</p> \[\theta \leftarrow \theta_0.\] <p>For minibatches $B\subset \mathcal{D}_{\mathrm{SFT}}$:</p> <ol> <li>Format each prompt-response pair as a single token sequence $z_i = (u_i,y_i).$</li> <li>Build a mask $M_i$.</li> <li> <p>Run the transformer on $z_i$ and compute logits at every position.</p> </li> <li>Compute the masked cross-entropy loss</li> </ol> \[\mathcal L_B(\theta) = - \frac{1}{\sum_{i\in B}\sum_t M_{i,t}} \sum_{i\in B} \sum_t M_{i,t} \log p_\theta(z_{i,t+1}\mid z_{i,\leq t}).\] <ol> <li>Update the parameters using gradient descent or one of its variants:</li> </ol> \[\theta \leftarrow \theta - \eta \nabla_\theta \mathcal L_B(\theta).\] <p>Return:</p> \[\theta_{\mathrm{SFT}}.\] <h1 id="why-does-sft-work-well">Why Does SFT Work Well?</h1> <p>SFT works well because it starts from a model that already understands a lot about language.</p> <p>During pretraining, the model has learned grammar, factual associations, code patterns, mathematical language, and many common ways in which text is structured. SFT does not need to teach all of this from scratch. Instead, it teaches the model how to use these abilities in an assistant-like setting.</p> <p>Before SFT, a prompt is mainly treated as text to continue. After SFT, the same prompt is more likely to be treated as an instruction to answer.</p> <p>This is why SFT can improve behaviors such as following instructions, answering directly, formatting responses clearly, maintaining a consistent assistant style, and adapting to multi-turn conversations.</p> <h1 id="limitations-of-sft">Limitations of SFT</h1> <p>SFT is a strong first step in post-training, but it also has important limitations.</p> <p>The first limitation is that SFT learns only from positive demonstrations. For each prompt $u_i$, the dataset usually contains a desired response $y_i$. The loss tells the model:</p> \[\text{increase the probability of } y_i.\] <p>However, it does not explicitly compare $y_i$ with other possible responses.</p> <p>For example, suppose we have two answers to the same prompt:</p> \[y^+ = \text{a clear, correct, helpful answer},\] <p>and</p> \[y^- = \text{a plausible but misleading answer}.\] <p>If SFT trains on $y^+$, it increases the likelihood of $y^+$. But it does not directly encode the preference relation</p> \[y^+ \succ y^-.\] <p>In other words, SFT shows the model what a good answer looks like, but it does not explicitly teach the model to distinguish it from worse alternatives.</p> <p>The second limitation is that the SFT objective is local, while many properties we care about are global.</p> <p>The loss is</p> \[- \sum_t \log p_\theta(y_t\mid u,y_{&lt;t}).\] <p>It trains the model to predict each token of the demonstrated answer. However, human judgments are usually about the answer as a whole:</p> \[\text{Was it helpful?}\] \[\text{Was it truthful?}\] \[\text{Was it safe?}\] \[\text{Was it too verbose, too vague, or too confident?}\] <p>These properties are not naturally assigned to individual tokens. They are global properties of the response.</p> <p>The third limitation is that many prompts have several good answers. SFT often treats one demonstration as the target, even though other responses could be equally good, or even better depending on the user’s intent. This can make the objective too narrow: the model learns to imitate the demonstrations, but not necessarily to choose the best response among several acceptable ones.</p> <p>The fourth limitation is distribution shift. The model may behave well on prompts similar to those in the SFT dataset, but users can ask unusual, ambiguous, adversarial, or highly specialized questions. In these cases, imitation of the training demonstrations may not be enough.</p> <p>Thus, SFT gives the model an important first notion of assistant-like behavior, but it does not fully solve the problem of aligning model outputs with human preferences.</p> <h1 id="from-demonstrations-to-preferences">From demonstrations to preferences</h1> <p>The limitations of SFT motivate the next stage of post-training. Instead of only showing the model examples of good answers, we can compare answers. Given a prompt $u$, suppose we have two candidate responses:</p> \[y^+ = \text{preferred response},\] <p>and</p> \[y^- = \text{rejected response}.\] <p>The supervision is now a preference relation</p> \[y^+ \succ y^-.\] <p>This kind of data contains information that SFT does not directly use. It tells the model not only what a good answer may look like, but also which answer is better when several possibilities are available.</p> <p>This leads to preference-based post-training methods such as reward modeling, reinforcement learning from human feedback, and direct preference optimization.</p> <p>These will be the topic of the next post.</p>]]></content><author><name></name></author><category term="machine-learning"/><category term="transformers"/><category term="post-training"/><category term="deep-learning"/><summary type="html"><![CDATA[Introduction In the previous post, we described the decoder-only Transformer architecture and the next-token prediction objective. We saw that an autoregressive language model takes a sequence of tokens and, at each position, outputs a probability distribution over the next token. This raises a natural question. If a model is trained only to predict the next token, why does it behave like an assistant?]]></summary></entry><entry><title type="html">Notes on Transformers</title><link href="https://mariacherifa.github.io/blog/2026/transformers/" rel="alternate" type="text/html" title="Notes on Transformers"/><published>2026-05-21T00:00:00+00:00</published><updated>2026-05-21T00:00:00+00:00</updated><id>https://mariacherifa.github.io/blog/2026/transformers</id><content type="html" xml:base="https://mariacherifa.github.io/blog/2026/transformers/"><![CDATA[<h1 id="introduction">Introduction</h1> <p>Transformers are at the heart of modern language models, but their inner workings can feel intimidating at first. The goal of this post is to make the main components of Transformers easier to understand, blending mathematical insights with intuition along the way.</p> <p>To keep things focused, we will look at <strong>the decoder-only Transformer architecture</strong>: the architecture behind autoregressive language models such as GPT-style models. The original Transformer was introduced with an encoder-decoder structure, but most modern language models for next-token prediction use a decoder-only design, where each layer gradually refines the representation of the input sequence.</p> <p>We will walk through the full pipeline step by step: starting from raw tokens, turning them into rich contextual representations through self-attention and MLP layers, and finally using those representations to predict the next token.</p> <h1 id="from-text-to-tokens">From Text to Tokens</h1> <p>To treat text as a mathematical object, we first need to define two fundamental notions: a <strong>vocabulary</strong> and a <strong>sequence of tokens</strong>.</p> <p>A vocabulary is a finite set of symbols used to represent text. These symbols can be characters, subwords, words, numbers, punctuation marks, or special tokens. We denote the vocabulary by</p> \[\mathcal{V} = \lbrace v_1, \dots, v_V \rbrace,\] <p>where $V = \lvert \mathcal{V}\rvert$ is the size of the vocabulary.</p> <p>We define an indexing function, $\mathrm{idx} : \mathcal{V} \to \lbrace 1,\dots,V\rbrace$ by,</p> \[\mathrm{idx}(v_i)=i.\] <p>This function maps each element in the vocabulary to its integer index. Conversely, we define a decoding function $\mathrm{tok}: \lbrace 1,\dots,V \rbrace \to \mathcal{V}$ by</p> \[\mathrm{tok}(i)=v_i.\] <p>Once a vocabulary and an indexing function are fixed, a text can be represented as a discrete sequence of token indices:</p> \[(x_1, \dots, x_T) \in \lbrace 1,\dots,V \rbrace^T.\] <p>Here, $T$ denotes the length of the tokenized text, and each integer $x_t$ corresponds to the token</p> \[\mathrm{tok}(x_t)=v_{x_t}.\] <p>In other words, $x_t$ is not the token itself, but the index of a token in the vocabulary.</p> <h1 id="from-tokens-to-embeddings">From Tokens to Embeddings</h1> <p>At this stage, a text is represented as a sequence of integers, $(x_1,\dots,x_T) \in \lbrace 1,\dots,V \rbrace^T$. However, an integer index is not a very meaningful object for a neural network. For example, if the token <code class="language-plaintext highlighter-rouge">"cat"</code> has index $42$ and the token <code class="language-plaintext highlighter-rouge">"dog"</code> has index $43$, this does not mean that <code class="language-plaintext highlighter-rouge">"dog"</code> is mathematically larger than <code class="language-plaintext highlighter-rouge">"cat"</code> or that they are close simply because their indices are close. Therefore, we need to transform each discrete token index into a vector representation. This is the role of <strong>the embedding layer</strong>.</p> <p>A first way to represent a token $x_t$ is through a one-hot vector. The one-hot vector associated with $x_t$ is denoted by $e_{x_t}$ and belongs to $\mathbb{R}^V$:</p> \[e_{x_t} = (0,\dots,0,1,0,\dots,0) \in \mathbb{R}^V.\] <p>The only nonzero coordinate is the one corresponding to the index $x_t$. This representation is useful because it identifies each token uniquely. However, one-hot vectors are high-dimensional, sparse, and do not encode semantic similarity between tokens. To obtain a dense and learnable representation, we introduce an embedding matrix:</p> \[E \in \mathbb{R}^{V \times d}.\] <p>Each row of $E$ corresponds to one token in the vocabulary and $d$ is the embedding dimension. The vector representation of token $x_t$ is obtained by selecting the row of $E$ corresponding to $x_t$:</p> \[z_t = e_{x_t}^{\top}E \in \mathbb{R}^d.\] <p>Equivalently, $z_t$ is simply the row of $E$ associated with the token index $x_t$.</p> <p>At this point, $z_t$ contains information about the identity of the token. However, it does not contain information about the position of the token in the sequence. This is an important issue for Transformers. Unlike recurrent neural networks, Transformers process all tokens in parallel, so they do not naturally know whether a token appears at the beginning, in the middle, or at the end of the sentence. For example, the sentences <code class="language-plaintext highlighter-rouge">"the cat chased the dog"</code> and <code class="language-plaintext highlighter-rouge">"the dog chased the cat"</code> contain almost the same tokens, but their meanings are different because the order of the tokens is different. Therefore, we need to add positional information to the token embeddings.</p> <p>To do this, we introduce a positional embedding vector $p_t \in \mathbb{R}^d$ for each position $t$. The initial representation of token $x_t$ is then obtained by adding its token embedding and its positional embedding:</p> \[h_t^{(0)} = z_t + p_t, \qquad h_t^{(0)}\in \mathbb{R}^d\] <p>In matrix form, if we define</p> \[Z = \begin{pmatrix} z_1 \\ z_2 \\ \vdots \\ z_T \end{pmatrix} \in \mathbb{R}^{T \times d}, \qquad P = \begin{pmatrix} p_1 \\ p_2 \\ \vdots \\ p_T \end{pmatrix} \in \mathbb{R}^{T \times d}\] <p>then the input matrix to the Transformer is:</p> \[H^{(0)} = Z + P \in \mathbb{R}^{T \times d}.\] <p>The notation $h_t^{(0)}$ means the initial hidden representation of the token at position $t$. The superscript $(0)$ indicates that this vector is the input representation before applying any Transformer layer. After the first Transformer block, it will become $h_t^{(1)}$; after the second block, $h_t^{(2)}$; and so on.</p> <p>So the embedding step maps each token and its position to a dense vector:</p> \[(x_t,t) \longmapsto h_t^{(0)} \in \mathbb{R}^d.\] <p>The matrix $E$ is not chosen manually. It is a learnable parameter of the model. At the beginning of training, its entries are initialized randomly. During training, the model updates $E$ using gradient descent, so that tokens used in similar contexts tend to acquire useful vector representations.</p> <p>The positional vectors $p_t$ can be defined in different ways. In some Transformers, they are also learned parameters, just like the token embeddings. In the original Transformer architecture, they were defined using sinusoidal functions, which gives the model a structured way to represent positions. In both cases, their role is the same: they inject information about the order of the tokens into the model.</p> <p>The dimension $d$ is a hyperparameter. It controls the size of the vector used to represent each token and each position. A small value of $d$ gives a compact representation, but may not be expressive enough. A large value of $d$ allows the model to store richer information, but increases the number of parameters and the computational cost. In practice, $d$ is chosen depending on the size of the model, the amount of data, and the available computational resources.</p> <h1 id="self-attention">Self-Attention</h1> <p>At this stage, the text is represented by a matrix:</p> \[H = \begin{pmatrix} h_1 \\ h_2 \\ \vdots \\ h_T \end{pmatrix} \in \mathbb{R}^{T \times d},\] <p>where each row $h_t \in \mathbb{R}^d$ is the embedding representation of the token $x_t$. For simplicity, in this section we write $H$ for the current hidden representation, which may be the initial embedding matrix $H^{(0)}$ or the output of a previous Transformer block.</p> <p>This gives us a vector representation of each token. However, these vectors are still not <strong>fully contextual</strong>. The representation of a token should not depend only on the token itself, but also on the other tokens around it. For example, the meaning of a word can change depending on the sentence in which it appears. Therefore, if we want to predict the next token or understand the structure of a sequence, we need representations that take the whole context into account.</p> <p>This is the main intuition behind self-attention: <strong>self-attention allows each token to build a new representation by looking at the other tokens in the same sequence</strong>.</p> <p>Mathematically, self-attention starts by constructing three matrices from $H$:</p> \[Q = HW_Q, \qquad W_Q \in \mathbb{R}^{d \times d_k},\] \[K = HW_K, \qquad W_K \in \mathbb{R}^{d \times d_k},\] \[V = HW_V, \qquad W_V \in \mathbb{R}^{d \times d_v}.\] <p>Therefore,</p> \[Q \in \mathbb{R}^{T \times d_k}, \qquad K \in \mathbb{R}^{T \times d_k}, \qquad V \in \mathbb{R}^{T \times d_v}.\] <p>The rows of these matrices are denoted by</p> \[Q = \begin{pmatrix} q_1 \\ q_2 \\ \vdots \\ q_T \end{pmatrix}, \qquad K = \begin{pmatrix} k_1 \\ k_2 \\ \vdots \\ k_T \end{pmatrix}, \qquad V = \begin{pmatrix} v_1 \\ v_2 \\ \vdots \\ v_T \end{pmatrix}.\] <p>The vectors $q_t$, $k_t$, and $v_t$ are called the <strong>query</strong>, <strong>key</strong>, and <strong>value</strong> vectors associated with token $x_t$.</p> <p>Intuitively, for a token $x_t$ represented by $h_t$:</p> <ul> <li>$q_t$ is the query: it represents what token $x_t$ is looking for in the other tokens.</li> <li>$k_s$ is the key of another token $x_s$: it represents what token $x_s$ offers, or how it can be matched.</li> <li>$v_s$ is the value of token $x_s$: it represents the information that token $x_s$ can contribute if it is considered relevant.</li> </ul> <p>So the self-attention mechanism answers the following question:</p> <blockquote> <p>For a given token $x_t$, which other tokens $x_s$ should it pay attention to in order to build a better contextual representation?</p> </blockquote> <p>To measure the relevance of token $x_s$ to token $x_t$, we compare the query $q_t$ with the key $k_s$. This comparison is done using a dot product:</p> \[q_t^\top k_s.\] <p>If this quantity is large, then token $x_s$ is highly relevant to token $x_t$. If it is small, then token $x_s$ is less relevant to token $x_t$.</p> <p>Thus, we define the attention score between token $t$ and token $s$ by:</p> \[a_{t,s} = \frac{q_t^\top k_s}{\sqrt{d_k}}.\] <p><strong>Remark.</strong> The normalization factor $\sqrt{d_k}$ is important when the dimension of the key and query vectors is large. Indeed, the dot product $q_t^\top k_s$ is obtained by summing over $d_k$ coordinates, so its magnitude tends to grow with $d_k$. Without this normalization, the scores could become too large in magnitude. This would make the softmax very sharp, meaning that most of the probability mass would be assigned to only a few tokens. Dividing by $\sqrt{d_k}$ keeps the scores on a more stable scale, leading to smoother attention weights and more stable optimization.</p> <p>For a fixed token $x_t$, we compute one score with every token in the sequence:</p> \[a_{t,1}, a_{t,2}, \dots, a_{t,T}.\] <p>These scores are then transformed into probabilities using the softmax function:</p> \[\alpha_{t,s} = \frac{\exp(a_{t,s})} {\sum_{r=1}^{T} \exp(a_{t,r})}.\] <p>The coefficients $\alpha_{t,s}$ are called <strong>attention weights</strong>. For a fixed $t$, they satisfy</p> \[\alpha_{t,s} \geq 0, \qquad \sum_{s=1}^{T} \alpha_{t,s}=1.\] <p>Therefore, for each token $x_t$, the attention weights define a probability distribution over all tokens in the sequence. The quantity $\alpha_{t,s}$ tells us how much token $x_t$ attends to token $x_s$.</p> <p>Once we have these attention weights, we compute the new representation of token $x_t$ as a weighted average of the value vectors:</p> \[\widetilde{h}_t = \sum_{s=1}^{T} \alpha_{t,s} v_s.\] <p>This vector $\widetilde{h}_t$ is the contextual representation of token $x_t$. It is no longer built only from the token itself. Instead, it is built from all tokens in the sequence, weighted by their relevance to $x_t$.</p> <p><strong>Remark.</strong> We can think of self-attention as producing a kernel-like representation of tokens, because each output vector is a weighted combination of value vectors. However, there is an important difference with classical kernel methods: here, the weights are not fixed by a predefined kernel. They are learned through the matrices $W_Q$, $W_K$, and $W_V$, and they also depend on the input sequence itself.</p> <p>In matrix form, all attention scores can be computed at once by taking the product between the query matrix and the transpose of the key matrix:</p> \[A = \frac{QK^\top}{\sqrt{d_k}} \in \mathbb{R}^{T \times T}.\] <p>The entry $A_{t,s}$ measures how much token $x_t$ is related to token $x_s$ before normalization. In other words, it is the raw attention score between the query of token $x_t$ and the key of token $x_s$.</p> <p>We then apply a softmax row by row to transform these scores into attention weights. Finally, these weights are used to take a weighted average of the value vectors:</p> \[\widetilde{H} = \mathrm{softmax} \left( \frac{QK^\top}{\sqrt{d_k}} \right) V.\] <p>This is the <strong>scaled dot-product self-attention</strong> formula.</p> <p>The important point is that each row $\widetilde{h}_t$ of $\widetilde{H}$ is a new representation of token $x_t$. It is built using information from the sequence, with more weight given to the tokens that are more relevant to $x_t$.</p> <p>So far, this attention mechanism allows every token to attend to every other token in the sequence. This is useful when the whole sequence is available, but it creates a problem for autoregressive language modeling. Indeed, in next-token prediction, the model should predict the future from the past, not by looking directly at the future. When predicting token $x_{t+1}$ from $x_1,\dots,x_t$, the representation at position $t$ should not use information from positions after $t$.</p> <p>This is why we need <strong>causal self-attention</strong>.</p> <h2 id="causal-self-attention">Causal Self-Attention</h2> <p>Causal self-attention is the same mechanism as self-attention, but with one important constraint: when computing the representation of token $x_t$, the model can only attend to tokens at positions $s \leq t$.</p> <p>To impose this constraint directly inside the attention computation, we modify the attention scores before applying the softmax. Recall that the attention score matrix is:</p> \[A = \frac{QK^\top}{\sqrt{d_k}}.\] <p>We introduce a mask matrix $M \in \mathbb{R}^{T \times T}$ defined by:</p> \[M_{t,s} = \begin{cases} 0 &amp; \text{if } s \leq t,\\ -\infty &amp; \text{if } s &gt; t. \end{cases}\] <p>After applying the softmax, the entries corresponding to future positions receive attention weight zero. Therefore, the causal self-attention formula becomes:</p> \[\widetilde{H} = \mathrm{softmax} \left( \frac{QK^\top}{\sqrt{d_k}} + M \right) V.\] <p>Thus, the representation of token $x_t$ is built only from $x_1,\dots,x_t$.</p> <h2 id="multi-head-attention">Multi-Head Attention</h2> <p>So far, we described self-attention as if the model had only one attention mechanism. In practice, Transformers use several attention mechanisms in parallel. This is called <strong>multi-head attention</strong>. The intuition is that one attention head may focus on one type of relationship between tokens, while another head may focus on a different type of relationship. For example, one head may learn to connect a pronoun to the noun it refers to, while another head may focus on local syntactic relations or long-range dependencies.</p> <p>Instead of computing a single set of queries, keys, and values, we compute several of them. For head $j$, we define</p> \[Q^{(j)} = HW_Q^{(j)}, \qquad K^{(j)} = HW_K^{(j)}, \qquad V^{(j)} = HW_V^{(j)}.\] <p>Each head then computes its own attention output:</p> \[O^{(j)} = \mathrm{softmax} \left( \frac{Q^{(j)}(K^{(j)})^\top}{\sqrt{d_k}} \right) V^{(j)}.\] <p>If we have $m$ heads, we obtain $m$ different outputs:</p> \[O^{(1)}, O^{(2)}, \dots, O^{(m)}.\] <p>These outputs are then concatenated and projected back to dimension $d$:</p> \[\mathrm{MultiHead}(H) = \mathrm{Concat} \left( O^{(1)},\dots,O^{(m)} \right) W_O.\] <p>The role of $W_O$ is to project the concatenated heads back to dimension $d$. Intuitively, it also mixes the information coming from the different heads into a single representation. Therefore,</p> \[\mathrm{MultiHead}(H) \in \mathbb{R}^{T \times d}.\] <p>The main idea is that multi-head attention allows the model to look at the sequence from several perspectives at the same time. Each head can learn a different way of comparing tokens and extracting contextual information.</p> <p>For the rest of the Transformer block, we denote the output of multi-head attention by:</p> \[\widetilde{H} = \mathrm{MultiHead}(H).\] <h2 id="residual-connection-and-layer-normalization">Residual Connection and Layer Normalization</h2> <p>The multi-head attention mechanism gives us a new contextual representation $\widetilde{H}$. However, in practice, Transformers do not simply replace $H$ by $\widetilde{H}$. Instead, they use two important operations: a <strong>residual connection</strong> and <strong>layer normalization</strong>.</p> <h3 id="why-do-we-need-a-residual-connection">Why Do We Need a Residual Connection?</h3> <p>The residual connection consists of adding the input representation back to the output of the attention layer:</p> \[H + \widetilde{H}.\] <p>The idea is simple but very important. The attention layer computes new information from the context, but we do not want the model to lose the previous representation of the tokens. By adding $H$ back, the model keeps direct access to what it already knew before attention.</p> <p>This is especially useful in deep networks. Without residual connections, information could gradually be distorted or lost as it passes through many layers. Residual connections create a direct path for information to flow from earlier layers to later layers. They also make optimization easier: instead of forcing each layer to learn a completely new representation from scratch, the layer only needs to learn a correction, or an update, to the previous representation.</p> <h3 id="why-do-we-need-layer-normalization">Why Do We Need Layer Normalization?</h3> <p>After the residual connection, we obtain</p> \[H_{\mathrm{res}} = H + \widetilde{H}.\] <p>This matrix contains one vector representation for each token. Since a Transformer is made of many successive blocks, these representations are transformed again and again by attention layers, MLPs, and residual additions. As a result, their numerical scale can drift during training. This can make optimization harder: the next layer has to process vectors whose coordinates may become too large, too small, or too variable from one layer to another. Layer normalization helps by keeping each token representation on a more controlled scale.</p> <p>The key idea is simple: for each token, we normalize its hidden vector across the feature dimension. So if</p> \[h_t = (h_{t,1},\dots,h_{t,d}) \in \mathbb{R}^d,\] <p>layer normalization first computes the mean and variance of the coordinates of this vector:</p> \[\mu_t = \frac{1}{d} \sum_{i=1}^{d} h_{t,i}, \qquad \sigma_t^2 = \frac{1}{d} \sum_{i=1}^{d} (h_{t,i}-\mu_t)^2.\] <p>Then each coordinate is centered and rescaled:</p> \[\widehat{h}_{t,i} = \frac{h_{t,i}-\mu_t}{\sqrt{\sigma_t^2+\varepsilon}}.\] <p>The small constant $\varepsilon$ is added only for numerical stability, to avoid division by zero.</p> <p>At this point, the normalized vector has a more stable scale. However, we do not want to force all hidden representations to always have the exact same scale and center. The model should still be able to choose the scale and shift that are useful for the task. For this reason, layer normalization introduces two learnable parameters:</p> \[\gamma \in \mathbb{R}^d \qquad \text{and} \qquad \beta \in \mathbb{R}^d.\] <p>The final layer-normalized vector is</p> \[\mathrm{LayerNorm}(h_t) = \gamma \odot \frac{h_t-\mu_t}{\sqrt{\sigma_t^2+\varepsilon}} + \beta.\] <p>Here, $\odot$ denotes coordinate-wise multiplication.</p> <p>The important point is that layer normalization is applied independently to each token representation. It normalizes the coordinates of a single hidden vector $h_t$, not the whole sequence at once.</p> <p>Intuitively, layer normalization says: before sending this representation to the next part of the Transformer, let us recenter and rescale it so that it is easier to process, while still allowing the model to learn the most useful scale through $\gamma$ and $\beta$.</p> <p>After the residual connection and layer normalization, the output of the attention sublayer is</p> \[H_{\mathrm{attn}} = \mathrm{LayerNorm} \left( H + \widetilde{H} \right).\] <h2 id="the-mlp-block">The MLP Block</h2> <p>After the attention sublayer, we obtain a contextual representation:</p> \[H_{\mathrm{attn}} \in \mathbb{R}^{T \times d}.\] <p>Each row of this matrix now contains information from the whole sequence. However, attention mainly allows tokens to exchange information with each other. Once this contextual information has been collected, we still need to process it and transform it in <strong>a richer way</strong>. This is the role of the MLP block.</p> <p>The MLP is applied independently to each token representation. If</p> \[H_{\mathrm{attn}} = \begin{pmatrix} h^{\mathrm{attn}}_1 \\ h^{\mathrm{attn}}_2 \\ \vdots \\ h^{\mathrm{attn}}_T \end{pmatrix},\] <p>then the same MLP is applied to every row $h^{\mathrm{attn}}_t$. A standard Transformer MLP has the form:</p> \[\mathrm{MLP}(h) = W_2 \, \phi(W_1 h + b_1) + b_2.\] <p>Here,</p> \[W_1 \in \mathbb{R}^{d_{\mathrm{ff}} \times d}, \qquad b_1 \in \mathbb{R}^{d_{\mathrm{ff}}},\] <p>and</p> \[W_2 \in \mathbb{R}^{d \times d_{\mathrm{ff}}}, \qquad b_2 \in \mathbb{R}^{d}.\] <p>The dimension $d_{\mathrm{ff}}$ is usually larger than $d$. For example, in many Transformer architectures, one takes $d_{\mathrm{ff}} = 4d$. So the MLP first expands the representation to a higher-dimensional space, applies a nonlinearity, and then projects it back to dimension $d$.</p> <p>The function $\phi$ is a nonlinear activation function, such as ReLU or GELU. This nonlinearity is important because without it, the MLP would only be a linear transformation. So for each token $x_t$, we compute</p> \[m_t = \mathrm{MLP}(h_t^{\mathrm{attn}}).\] <p>In matrix form, this can be written as</p> \[M = \mathrm{MLP}(H_{\mathrm{attn}}),\] <p>where the MLP is applied row by row.</p> <p>Intuitively, attention answers the question:</p> <blockquote> <p>Which tokens should I look at?</p> </blockquote> <p>The MLP answers a different question:</p> <blockquote> <p>Once I have collected contextual information, how should I transform it?</p> </blockquote> <p>So attention mixes information across tokens, while the MLP enriches the representation of each token independently.</p> <p>As before, Transformers add a residual connection and layer normalization around the MLP block. Therefore, the output of one Transformer block can be written as</p> \[H_{\mathrm{out}} = \mathrm{LayerNorm} \left( H_{\mathrm{attn}} + \mathrm{MLP}(H_{\mathrm{attn}}) \right).\] <p>This gives us a new matrix</p> \[H_{\mathrm{out}} \in \mathbb{R}^{T \times d},\] <p>where each row is a richer contextual representation of the corresponding token.</p> <p>If we stack several Transformer blocks, this process is repeated. Each layer refines the representation by first allowing tokens to communicate through attention, and then transforming each token representation through an MLP.</p> <p>The whole point of the Transformer is therefore <strong>to build good contextual representations of the sequence</strong>.</p> <h1 id="next-token-prediction-and-training-objective">Next-Token Prediction and Training Objective</h1> <p>Until now, we have described how a Transformer takes a sequence of tokens and builds contextual representations. The main idea is that each token representation is progressively updated through self-attention, residual connections, layer normalization, and MLP blocks.</p> <p>But why do we build these representations?</p> <p>For language modeling, the goal is to predict the next token.</p> <p>Assume we have a sequence of tokens</p> \[x_1, x_2, \dots, x_T,\] <p>where each token belongs to a vocabulary of size $V$. The autoregressive modeling objective is to learn, for each position $t$, the conditional probability</p> \[p_\theta(x_{t+1} \mid x_1,\dots,x_t).\] <p>Intuitively, the model tries to answer the following question:</p> <blockquote> <p>Given the previous tokens $x_1,\dots,x_t$, what is the probability distribution of the next token $x_{t+1}$?</p> </blockquote> <p>This is why the final hidden representations are important. After passing the sequence through the Transformer, we obtain contextual representations:</p> \[h_1, h_2, \dots, h_T.\] <p>The vector $h_t$ summarizes the information available at position $t$. In an autoregressive Transformer, it is used to predict the next token $x_{t+1}$.</p> <p>To do this, we map $h_t$ to a vector of logits over the vocabulary:</p> \[\ell_t = W_{\mathrm{out}}h_t + b_{\mathrm{out}},\] <p>where $W_{\mathrm{out}}\in\mathbb{R}^{V\times d}$ and $b_{\mathrm{out}}\in\mathbb{R}^{V}$ are learned parameters: $W_{\mathrm{out}}$ maps the hidden representation $h_t\in\mathbb{R}^d$ to one score per vocabulary token, and $b_{\mathrm{out}}$ adds a learned bias to each token score.</p> <p>Thus, \(\ell_t \in \mathbb{R}^{V}.\)</p> <p>Each coordinate $\ell_{t,i}$ is a score associated with token $i$ in the vocabulary. These scores are not probabilities yet: they can be negative, and they do not sum to one. To transform them into probabilities, we apply the softmax function:</p> \[p_\theta(x_{t+1}=i \mid x_1,\dots,x_t) = \frac{\exp(\ell_{t,i})} {\sum_{j=1}^{V}\exp(\ell_{t,j})}.\] <p>Therefore, for each position $t$, the Transformer outputs a probability distribution over the whole vocabulary:</p> \[p_\theta(\cdot \mid x_1,\dots,x_t) \in \Delta^{V-1},\] <p>where $\Delta^{V-1}$ is the probability simplex over the vocabulary:</p> \[\Delta^{V-1} = \left\{ p \in \mathbb{R}^{V} : p_i \geq 0 \quad \text{and} \quad \sum_{i=1}^{V} p_i = 1 \right\}.\] <p>So mathematically, we can think of the Transformer as a parameterized map</p> \[(x_1,\dots,x_t) \longmapsto p_\theta(\cdot \mid x_1,\dots,x_t) \in \Delta^{V-1}.\] <p>The output is not directly a single token. It is first a probability distribution over all possible next tokens.</p> <h2 id="factorization-of-the-sequence-probability">Factorization of the Sequence Probability</h2> <p>For a full sequence $x_1,\dots,x_T$, an autoregressive language model factorizes the joint probability as</p> \[p_\theta(x_1,\dots,x_T) = \prod_{t=1}^{T} p_\theta(x_t \mid x_1,\dots,x_{t-1}) = \prod_{t=1}^{T} p_\theta(x_t \mid x_{&lt;t}).\] <p>This means that the model assigns a probability to the whole sequence by predicting each token from the tokens that came before it. So next-token prediction is not only a generation rule. It also defines a probability model over entire sequences.</p> <h2 id="training-objective">Training Objective</h2> <p>During training, we already know the true next token. For each position $t$, the model predicts a probability distribution</p> \[p_\theta(\cdot \mid x_{&lt;t}),\] <p>and we want the model to assign high probability to the true token $x_t$. Therefore, we maximize the likelihood of the training sequence:</p> \[\prod_{t=1}^{T} p_\theta(x_t \mid x_{&lt;t}).\] <p>Equivalently, we minimize the negative log-likelihood:</p> \[\mathcal{L}(\theta) = - \sum_{t=1}^{T} \log p_\theta(x_t \mid x_{&lt;t}).\] <p>This is the usual cross-entropy loss used to train language models.</p> <p>Intuitively, if the true token is $x_t$, then the loss penalizes the model when it assigns a small probability to $x_t$. Training pushes the model to put more probability mass on the correct next token.</p> <h2 id="what-are-the-parameters-theta">What Are the Parameters $\theta$?</h2> <p>The notation $\theta$ denotes all the trainable parameters of the Transformer. More precisely, it includes:</p> <ul> <li>the token embedding matrix $E$;</li> <li>the positional embeddings, if they are learned;</li> <li>the attention matrices $W_Q$, $W_K$, and $W_V$ in each layer;</li> <li>the output projection matrices used after attention;</li> <li>the MLP weights and biases in each Transformer block;</li> <li>the layer normalization parameters $\gamma$ and $\beta$;</li> <li>the final output matrix $W_{\mathrm{out}}$ and bias $b_{\mathrm{out}}$.</li> </ul> <p>So $\theta$ represents everything the model learns during training.</p> <p>The role of training is to adjust these parameters so that the probability distribution</p> \[p_\theta(\cdot \mid x_1,\dots,x_t)\] <p>becomes good at predicting the next token.</p> <p>This is the central idea behind autoregressive language modeling: the Transformer builds a contextual representation of the previous tokens, then uses this representation to produce a probability distribution over the next token.</p> <p>The overall pipeline can be summarized as follows:</p> <div style="text-align:center; line-height:2; font-size:1.05em; margin: 2em 0;"> <div><strong>Tokens</strong></div> <div>↓</div> <div><strong>Token embeddings + positional embeddings</strong></div> <div>↓</div> <div><strong>Causal self-attention</strong></div> <div>↓</div> <div><strong>Residual connection + layer normalization</strong></div> <div>↓</div> <div><strong>MLP</strong></div> <div>↓</div> <div><strong>Residual connection + layer normalization</strong></div> <div>↓</div> <div><strong>Logits over vocabulary</strong></div> <div>↓</div> <div><strong>Softmax</strong></div> <div>↓</div> <div><strong>Next-token distribution</strong></div> </div> <h1 id="what-comes-next">What comes next?</h1> <p>In this post, we described the transformer architecture and the next-token prediction objective used to train language models.</p> <p>However, pretraining alone does not fully explain why modern LLMs behave like assistants. A pretrained model learns to continue text; an assistant must follow instructions, answer helpfully, refuse unsafe requests, and adapt to user intent.</p> <p>This gap is addressed by post-training methods such as supervised fine-tuning, preference optimization, RLHF, and safety tuning. These will be the topic of the next post.</p>]]></content><author><name></name></author><category term="machine-learning"/><category term="transformers"/><category term="attention"/><category term="deep-learning"/><summary type="html"><![CDATA[Introduction]]></summary></entry></feed>